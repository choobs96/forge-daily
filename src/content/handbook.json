[
 {
  "id": "m0",
  "title": "The Roadmap & Mindset Shift",
  "subtitle": "Why you already have 60% of the skills — and what the remaining 40% actually looks like.",
  "html": "<p class=\"lead\">A Data Analyst answers <em>\"what's happening?\"</em> An Analytics Engineer builds the <em>system</em> that lets the whole company answer that question, repeatedly, correctly, and fast. Same SQL — different muscle.</p>\n\n  <h3>The Three Roles, Disambiguated</h3>\n  <table>\n    <tbody><tr><th>Role</th><th>Primary Output</th><th>Owns</th><th>Mental Model</th></tr>\n    <tr><td><b>Data Analyst</b></td><td>Insights, dashboards, ad-hoc queries</td><td>The question</td><td>\"Why did X drop?\"</td></tr>\n    <tr><td><b>Analytics Engineer</b></td><td>Trusted models, semantic layer, tests</td><td>The transformation</td><td>\"Is this metric correct &amp; reproducible?\"</td></tr>\n    <tr><td><b>Data Engineer</b></td><td>Pipelines, infra, ingestion, streaming</td><td>The plumbing</td><td>\"Does data arrive on time, every time?\"</td></tr>\n  </tbody></table>\n\n  <h3>Your Current Advantage (you already have these)</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>SQL fluency</h4>\n      <p>You already write joins, CTEs, window functions. You just need to lift it from \"answering questions\" to \"modeling reality.\"</p>\n    </div>\n    <div class=\"card\">\n      <h4>Business context</h4>\n      <p>You know what a \"lead\", \"contractor\", \"renewal\" means in production. AEs spend years learning what analysts already know.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Stakeholder muscle</h4>\n      <p>You translate vague asks → precise specs. That's harder than dbt.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Tool exposure</h4>\n      <p>Databricks, dbt, Salesforce, Tableau — you've been touching the production stack.</p>\n    </div>\n  </div>\n\n  <h3>What You're Adding</h3>\n  <div class=\"fastcards\">\n    <div class=\"fastcard\"><div class=\"term\">Modeling discipline</div><div class=\"def\">Kimball dimensional modeling, SCDs, fact/dim grain</div></div>\n    <div class=\"fastcard\"><div class=\"term\">Software craft</div><div class=\"def\">Git, PRs, modular code, code review, testing</div></div>\n    <div class=\"fastcard\"><div class=\"term\">dbt as a system</div><div class=\"def\">refs, macros, snapshots, incremental, exposures</div></div>\n    <div class=\"fastcard\"><div class=\"term\">Warehouse internals</div><div class=\"def\">Columnar, partitioning, clustering, file formats</div></div>\n    <div class=\"fastcard\"><div class=\"term\">Orchestration</div><div class=\"def\">Airflow/Dagster DAGs, dependencies, SLAs</div></div>\n    <div class=\"fastcard\"><div class=\"term\">Spark / Databricks</div><div class=\"def\">Delta Lake, photon, broadcast joins, AQE</div></div>\n    <div class=\"fastcard\"><div class=\"term\">CDC &amp; ingestion</div><div class=\"def\">Kafka, Debezium, Fivetran, soft deletes</div></div>\n    <div class=\"fastcard\"><div class=\"term\">Python for DE</div><div class=\"def\">pandas → PySpark, requests, boto3, click CLIs</div></div>\n    <div class=\"fastcard\"><div class=\"term\">Cost &amp; perf</div><div class=\"def\">Query optimisation, warehouse sizing, partitioning</div></div>\n    <div class=\"fastcard\"><div class=\"term\">System design</div><div class=\"def\">Lakehouse, medallion, lineage, contracts</div></div>\n  </div>\n\n  <h3>Suggested 12-Week Plan</h3>\n  <table>\n    <tbody><tr><th>Weeks</th><th>Focus</th><th>Outcome</th></tr>\n    <tr><td>1–2</td><td>Advanced SQL + Git</td><td>Comfortable with window functions, branches, PRs</td></tr>\n    <tr><td>3–4</td><td>Data modeling + dbt basics</td><td>Build a 3-layer dbt project from scratch</td></tr>\n    <tr><td>5–6</td><td>dbt advanced + warehouse internals</td><td>Snapshots, incremental, macros; understand partitions</td></tr>\n    <tr><td>7–8</td><td>Spark/Databricks + Python</td><td>Read Spark plans; write a PySpark job</td></tr>\n    <tr><td>9–10</td><td>Orchestration + CDC</td><td>Build an Airflow DAG; explain Kafka end-to-end</td></tr>\n    <tr><td>11–12</td><td>System design + portfolio</td><td>Public dbt project on GitHub; mock interview</td></tr>\n  </tbody></table>\n\n  <h3>Quick Flashcards</h3>\n  <div class=\"flashcard-set\">\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click to flip</div>\n      <div class=\"question\">What's the single biggest mindset shift from Analyst → AE?</div>\n      <div class=\"answer\"><b>From answering questions to building systems.</b> An analyst's deliverable is the answer. An AE's deliverable is the durable model that lets <em>anyone</em> get that answer correctly tomorrow.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click to flip</div>\n      <div class=\"question\">Why is Analytics Engineering a discipline at all (vs just \"good analysts\")?</div>\n      <div class=\"answer\">Because the volume of data + stakeholders + tools exploded. The cost of one bad metric (e.g. Sales bonus tied to wrong revenue) is millions. AE applies <em>software engineering rigor</em> — version control, testing, modularity — to data transformation.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click to flip</div>\n      <div class=\"question\">What's the difference between an Analytics Engineer and a Data Engineer?</div>\n      <div class=\"answer\">AE owns <b>transformation</b> (warehouse SQL, dbt, semantic layer). DE owns <b>ingestion + infrastructure</b> (Kafka, Spark jobs, Airflow infra, cloud). AEs are SQL-first; DEs are Python/Scala-first. Overlap exists.</div>\n    </div>\n  </div>\n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>AE = software engineering discipline applied to data transformation.</li>\n      <li>You already have SQL + business context — the rare half. Layer modeling + craft + tools on top.</li>\n      <li>Fix upstream in the model, not downstream in the dashboard.</li>\n      <li>12-week realistic timeline — depth beats speed.</li>\n    </ul>\n  </div>",
  "words": 459
 },
 {
  "id": "m1",
  "title": "Advanced SQL",
  "subtitle": "From \"answers a question\" to \"production-quality, performant, deterministic.\"",
  "html": "<p class=\"lead\">SQL is 70% of the AE job. You probably already write it daily — but there's a gap between \"works for this report\" and \"works in a 100M-row Spark cluster with 12 downstream models depending on it.\"</p>\n\n  <h3>1. CTEs vs Subqueries — the Style You'll Be Judged On</h3>\n  <p>Every dbt model you'll touch uses CTEs. Subqueries are read once, top-to-bottom; nested subqueries are unreadable.</p>\n  <pre><code><span class=\"cm\">-- 🟢 AE style</span>\n<span class=\"kw\">with</span> active_jobs <span class=\"kw\">as</span> (\n  <span class=\"kw\">select</span> job_id, account_id, created_at\n  <span class=\"kw\">from</span> <span class=\"fn\">{{ ref('stg_jobs') }}</span>\n  <span class=\"kw\">where</span> __is_deleted = <span class=\"kw\">false</span>\n),\njob_counts <span class=\"kw\">as</span> (\n  <span class=\"kw\">select</span> account_id, <span class=\"fn\">count</span>(*) <span class=\"kw\">as</span> job_count\n  <span class=\"kw\">from</span> active_jobs\n  <span class=\"kw\">group by</span> account_id\n)\n<span class=\"kw\">select</span> * <span class=\"kw\">from</span> job_counts <span class=\"kw\">where</span> job_count &gt; <span class=\"num\">5</span>;</code></pre>\n\n  <div class=\"callout warn\">\n    <div class=\"callout-title\">Production gotcha</div>\n    Always filter <code>__is_deleted = false</code> on Kafka CDC tables (jobs, accounts, sites, contacts, etc.). Missing it = production correctness bug.\n  </div>\n\n  <h3>2. Window Functions — the AE Power Tool</h3>\n  <p>Window functions let you do <em>row-level + aggregate</em> together. They're how every \"first/last\", \"running total\", \"rank\", \"lag/lead\" question gets solved.</p>\n\n  <h4>The anatomy</h4>\n  <pre><code><span class=\"fn\">function</span>() <span class=\"kw\">over</span> (\n  <span class=\"kw\">partition by</span> &lt;reset group&gt;\n  <span class=\"kw\">order by</span> &lt;ordering within group&gt;\n  <span class=\"kw\">rows between</span> &lt;frame&gt;\n)</code></pre>\n\n  <h4>The seven you must know cold</h4>\n  <table>\n    <tbody><tr><th>Function</th><th>Use Case</th></tr>\n    <tr><td><code>row_number()</code></td><td>Dedupe — keep latest row per key</td></tr>\n    <tr><td><code>rank()</code> / <code>dense_rank()</code></td><td>Leaderboards, ties handling</td></tr>\n    <tr><td><code>lag()</code> / <code>lead()</code></td><td>Period-over-period, \"previous status\"</td></tr>\n    <tr><td><code>sum() over</code></td><td>Running total / cumulative</td></tr>\n    <tr><td><code>first_value()</code> / <code>last_value()</code></td><td>First/last status, signup channel</td></tr>\n    <tr><td><code>ntile(n)</code></td><td>Quartile, decile buckets</td></tr>\n    <tr><td><code>count(*) over (partition by)</code></td><td>\"How many siblings does this row have\"</td></tr>\n  </tbody></table>\n\n  <h4>The classic dedupe pattern (memorise this)</h4>\n  <pre><code><span class=\"kw\">with</span> ranked <span class=\"kw\">as</span> (\n  <span class=\"kw\">select</span> *,\n    <span class=\"fn\">row_number</span>() <span class=\"kw\">over</span> (\n      <span class=\"kw\">partition by</span> account_id\n      <span class=\"kw\">order by</span> updated_at <span class=\"kw\">desc</span>\n    ) <span class=\"kw\">as</span> rn\n  <span class=\"kw\">from</span> raw.accounts\n)\n<span class=\"kw\">select</span> * <span class=\"kw\">from</span> ranked <span class=\"kw\">where</span> rn = <span class=\"num\">1</span>;</code></pre>\n\n  <h3>3. Joins — beyond INNER/LEFT</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>SEMI JOIN <span class=\"level-badge level-intermediate\">int</span></h4>\n      <p>\"Rows in A that <em>have a match</em> in B\" — no row multiplication, no columns from B.</p>\n      <pre><code><span class=\"kw\">select</span> * <span class=\"kw\">from</span> jobs j\n<span class=\"kw\">where exists</span> (\n  <span class=\"kw\">select</span> <span class=\"num\">1</span> <span class=\"kw\">from</span> bookings b\n  <span class=\"kw\">where</span> b.job_id = j.id\n);</code></pre>\n    </div>\n    <div class=\"card\">\n      <h4>ANTI JOIN <span class=\"level-badge level-intermediate\">int</span></h4>\n      <p>\"Rows in A with <em>no match</em> in B.\" Use <code>NOT EXISTS</code> over <code>NOT IN</code> (NULL-safe).</p>\n      <pre><code><span class=\"kw\">select</span> * <span class=\"kw\">from</span> accounts a\n<span class=\"kw\">where not exists</span> (\n  <span class=\"kw\">select</span> <span class=\"num\">1</span> <span class=\"kw\">from</span> jobs j\n  <span class=\"kw\">where</span> j.account_id = a.id\n);</code></pre>\n    </div>\n    <div class=\"card\">\n      <h4>Cross / Cartesian <span class=\"level-badge level-advanced\">adv</span></h4>\n      <p>Multiplies every row of A by every row of B. Useful for date spines, cohort grids — dangerous otherwise.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Lateral / Cross Apply <span class=\"level-badge level-advanced\">adv</span></h4>\n      <p>Per-row subquery that can reference outer columns. In Spark: <code>LATERAL VIEW</code> for exploding arrays.</p>\n    </div>\n  </div>\n\n  <h3>4. Join Fan-Out — the silent killer</h3>\n  <p>If <code>orders</code> has 100 rows and <code>order_items</code> has 500 rows, joining them produces 500 rows. SUM(order_value) is now <em>5× the truth</em>.</p>\n  <pre><code><span class=\"cm\">-- ❌ wrong: order_value is duplicated per item</span>\n<span class=\"kw\">select</span> <span class=\"fn\">sum</span>(o.order_value)\n<span class=\"kw\">from</span> orders o\n<span class=\"kw\">join</span> order_items i <span class=\"kw\">on</span> i.order_id = o.id;\n\n<span class=\"cm\">-- ✅ correct: aggregate one side before joining</span>\n<span class=\"kw\">with</span> items_per_order <span class=\"kw\">as</span> (\n  <span class=\"kw\">select</span> order_id, <span class=\"fn\">count</span>(*) <span class=\"kw\">as</span> item_count\n  <span class=\"kw\">from</span> order_items <span class=\"kw\">group by</span> order_id\n)\n<span class=\"kw\">select</span> <span class=\"fn\">sum</span>(o.order_value), <span class=\"fn\">sum</span>(i.item_count)\n<span class=\"kw\">from</span> orders o\n<span class=\"kw\">left join</span> items_per_order i <span class=\"kw\">on</span> i.order_id = o.id;</code></pre>\n  <div class=\"callout warn\">\n    <div class=\"callout-title\">Always test grain after a join</div>\n    Run <code>count(*) vs count(distinct pk)</code> after every join. If they differ, you have fan-out.\n  </div>\n\n  <h3>5. NULL Handling — the trap</h3>\n  <ul>\n    <li><code>NULL = NULL</code> is <b>NULL</b>, not true. Use <code>IS NULL</code>.</li>\n    <li><code>NOT IN (subquery)</code> returns <b>zero rows</b> if the subquery has any NULL. Use <code>NOT EXISTS</code>.</li>\n    <li><code>COUNT(*)</code> counts NULLs; <code>COUNT(col)</code> ignores them.</li>\n    <li><code>SUM(NULL + 1) = NULL</code> — wrap in <code>COALESCE</code>.</li>\n  </ul>\n\n  <h3>6. Pivots &amp; Unpivots</h3>\n  <pre><code><span class=\"cm\">-- pivot via conditional aggregation</span>\n<span class=\"kw\">select</span>\n  account_id,\n  <span class=\"fn\">sum</span>(<span class=\"kw\">case when</span> status = <span class=\"str\">'won'</span> <span class=\"kw\">then</span> <span class=\"num\">1</span> <span class=\"kw\">else</span> <span class=\"num\">0</span> <span class=\"kw\">end</span>) <span class=\"kw\">as</span> wins,\n  <span class=\"fn\">sum</span>(<span class=\"kw\">case when</span> status = <span class=\"str\">'lost'</span> <span class=\"kw\">then</span> <span class=\"num\">1</span> <span class=\"kw\">else</span> <span class=\"num\">0</span> <span class=\"kw\">end</span>) <span class=\"kw\">as</span> losses\n<span class=\"kw\">from</span> deals\n<span class=\"kw\">group by</span> account_id;</code></pre>\n\n  <h3>7. Query Plans — read them, don't fear them</h3>\n  <p>In Databricks: <code>EXPLAIN FORMATTED &lt;query&gt;</code>. Top → bottom = inside → outside.</p>\n  <ul>\n    <li><b>Scan</b> — reading a table. Watch <code>PartitionFilters</code>.</li>\n    <li><b>BroadcastHashJoin</b> — small table broadcast to all nodes. Fast.</li>\n    <li><b>SortMergeJoin</b> — both sides large; shuffled. Slow but unavoidable.</li>\n    <li><b>Exchange (Shuffle)</b> — repartitioning. Expensive — minimise.</li>\n    <li><b>Filter</b> — predicate push-down should put it as close to Scan as possible.</li>\n  </ul>\n\n  <h3>SQL Performance Cheat Sheet</h3>\n  <div class=\"cheatsheet\">\n<span class=\"comment\"># Predicates push down — filter EARLY</span>\n<span class=\"label\">WHERE</span> date &gt;= '2026-01-01'   <span class=\"comment\">→ before any JOIN</span>\n\n<span class=\"comment\"># Project only what you need</span>\n<span class=\"label\">SELECT</span> id, name        <span class=\"comment\">→ not SELECT *</span>\n\n<span class=\"comment\"># Aggregate before joining (avoid fan-out)</span>\n\n<span class=\"comment\"># Use EXISTS over IN for nullable subqueries</span>\n\n<span class=\"comment\"># Avoid functions on join keys (kills partition pruning)</span>\nON a.id = b.id              <span class=\"comment\">✅</span>\nON UPPER(a.id) = b.id       <span class=\"comment\">❌</span>\n\n<span class=\"comment\"># Broadcast small dim tables explicitly when planner misses</span>\n<span class=\"label\">SELECT /*+ BROADCAST(dim) */</span> ...\n  </div>\n\n  <h3>Flashcards</h3>\n  <div class=\"flashcard-set\">\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">What does <code>ROW_NUMBER() OVER (PARTITION BY x ORDER BY y DESC)</code> achieve when filtered to <code>= 1</code>?</div>\n      <div class=\"answer\">Returns the latest (by y) row per x — the canonical dedupe pattern.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">Why is <code>NOT IN (subquery)</code> dangerous?</div>\n      <div class=\"answer\">If the subquery returns any NULL, the entire predicate becomes NULL → no rows match. Use <code>NOT EXISTS</code>.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">What's the difference between <code>RANK()</code> and <code>DENSE_RANK()</code>?</div>\n      <div class=\"answer\">RANK leaves gaps after ties (1, 2, 2, 4); DENSE_RANK doesn't (1, 2, 2, 3).</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">How do you detect a join fan-out before SUM gives you wrong numbers?</div>\n      <div class=\"answer\">Compare <code>count(*)</code> vs <code>count(distinct primary_key)</code> on the result. If they differ, fan-out occurred.</div>\n    </div>\n  </div>\n\n  \n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>Always-CTE style: every dbt model is CTEs top-to-bottom.</li>\n      <li>Window functions are the AE power tool — master the seven core ones.</li>\n      <li>Fan-out is the #1 bug source: <code>count(*) vs count(distinct pk)</code> after joins.</li>\n      <li>NULL traps: <code>= NULL</code>, <code>NOT IN</code>, <code>COUNT(col)</code>.</li>\n      <li>Read query plans — push filters down, project less, broadcast small dims.</li>\n      <li>In a CDC pipeline: <code>__is_deleted = false</code> on CDC tables, always.</li>\n    </ul>\n  </div>",
  "words": 889
 },
 {
  "id": "m2",
  "title": "Python for Data Engineering",
  "subtitle": "You don't need to be a software engineer. You need to be dangerous in 5 specific areas.",
  "html": "<p class=\"lead\">As an AE you'll write Python for: glue scripts, custom dbt macros sometimes, PySpark, data quality checks, API ingestion, and CLI tools. ~80% of the language you can ignore.</p>\n\n  <h3>The 5 Areas That Matter</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>1. Data structures</h4>\n      <p>dict, list, tuple, set. List/dict comprehensions. <code>collections.defaultdict</code>, <code>Counter</code>.</p>\n    </div>\n    <div class=\"card\">\n      <h4>2. pandas (then PySpark)</h4>\n      <p>DataFrame ops, groupby, merge, pivot, apply. Same mental model as SQL.</p>\n    </div>\n    <div class=\"card\">\n      <h4>3. requests / APIs</h4>\n      <p>GET/POST, auth headers, pagination, retries with backoff.</p>\n    </div>\n    <div class=\"card\">\n      <h4>4. SQLAlchemy / DB drivers</h4>\n      <p>Connect to Postgres/Databricks. Parameterised queries. Connection pooling basics.</p>\n    </div>\n    <div class=\"card\">\n      <h4>5. CLI + env</h4>\n      <p>argparse / click for scripts. <code>.env</code> with python-dotenv. <code>os.environ</code>.</p>\n    </div>\n  </div>\n\n  <h3>SQL → Pandas Translation Table</h3>\n  <table>\n    <tbody><tr><th>SQL</th><th>Pandas</th></tr>\n    <tr><td><code>SELECT col FROM t</code></td><td><code>df['col']</code> or <code>df[['col']]</code></td></tr>\n    <tr><td><code>WHERE x &gt; 5</code></td><td><code>df[df['x'] &gt; 5]</code></td></tr>\n    <tr><td><code>GROUP BY x AGG SUM(y)</code></td><td><code>df.groupby('x')['y'].sum()</code></td></tr>\n    <tr><td><code>JOIN ON id</code></td><td><code>df1.merge(df2, on='id', how='left')</code></td></tr>\n    <tr><td><code>ORDER BY y DESC</code></td><td><code>df.sort_values('y', ascending=False)</code></td></tr>\n    <tr><td><code>UNION ALL</code></td><td><code>pd.concat([df1, df2])</code></td></tr>\n    <tr><td><code>ROW_NUMBER() OVER ...</code></td><td><code>df.groupby('x').cumcount() + 1</code></td></tr>\n    <tr><td><code>CASE WHEN</code></td><td><code>np.where(cond, a, b)</code> or <code>pd.cut</code></td></tr>\n  </tbody></table>\n\n  <h3>Pandas → PySpark — what changes</h3>\n  <p>PySpark is pandas-adjacent but lazy (no execution until <code>.show()</code> / <code>.collect()</code>) and distributed. Key differences:</p>\n  <pre><code><span class=\"cm\"># pandas</span>\ndf.groupby(<span class=\"str\">'account_id'</span>)[<span class=\"str\">'value'</span>].sum()\n\n<span class=\"cm\"># PySpark</span>\n<span class=\"kw\">from</span> pyspark.sql <span class=\"kw\">import</span> functions <span class=\"kw\">as</span> F\ndf.groupBy(<span class=\"str\">'account_id'</span>).agg(F.sum(<span class=\"str\">'value'</span>))\n\n<span class=\"cm\"># PySpark filter</span>\ndf.filter(F.col(<span class=\"str\">'date'</span>) &gt;= <span class=\"str\">'2026-01-01'</span>)</code></pre>\n\n  <h3>Pattern: Idempotent script structure</h3>\n  <pre><code><span class=\"kw\">import</span> click\n<span class=\"kw\">from</span> datetime <span class=\"kw\">import</span> date\n\n<span class=\"cm\">@click.command()</span>\n<span class=\"cm\">@click.option('--run-date', default=str(date.today()))</span>\n<span class=\"cm\">@click.option('--full-refresh', is_flag=True)</span>\n<span class=\"kw\">def</span> <span class=\"fn\">main</span>(run_date, full_refresh):\n    <span class=\"str\">\"\"\"ETL the X table for run_date.\"\"\"</span>\n    logger.info(<span class=\"str\">f\"Running for {run_date}\"</span>)\n    df = extract(run_date)\n    df = transform(df)\n    load(df, run_date, full_refresh=full_refresh)\n\n<span class=\"kw\">if</span> __name__ == <span class=\"str\">'__main__'</span>:\n    main()</code></pre>\n\n  <h3>Pattern: API ingestion with retries</h3>\n  <pre><code><span class=\"kw\">import</span> requests\n<span class=\"kw\">from</span> tenacity <span class=\"kw\">import</span> retry, wait_exponential, stop_after_attempt\n\n<span class=\"cm\">@retry(wait=wait_exponential(multiplier=1, max=30), stop=stop_after_attempt(5))</span>\n<span class=\"kw\">def</span> <span class=\"fn\">fetch_page</span>(url, params):\n    r = requests.get(url, params=params, timeout=<span class=\"num\">30</span>)\n    r.raise_for_status()\n    <span class=\"kw\">return</span> r.json()</code></pre>\n\n  <h3>Fast-Pointer Cards</h3>\n  <div class=\"fastcards\">\n    <div class=\"fastcard\"><div class=\"term\">venv / poetry</div><div class=\"def\">Isolate deps per project. Never <code>pip install</code> globally.</div></div>\n    <div class=\"fastcard\"><div class=\"term\">f-strings</div><div class=\"def\"><code>f\"Hello {name}\"</code> — modern formatting</div></div>\n    <div class=\"fastcard\"><div class=\"term\">type hints</div><div class=\"def\"><code>def f(x: int) -&gt; str</code> — reviewers will ask for them</div></div>\n    <div class=\"fastcard\"><div class=\"term\">logging &gt; print</div><div class=\"def\"><code>logger.info</code>, never <code>print()</code> in production</div></div>\n    <div class=\"fastcard\"><div class=\"term\">pathlib &gt; os.path</div><div class=\"def\"><code>Path(\"data\") / \"raw.csv\"</code></div></div>\n    <div class=\"fastcard\"><div class=\"term\">try/except/finally</div><div class=\"def\">Catch specific exceptions, not bare <code>except:</code></div></div>\n    <div class=\"fastcard\"><div class=\"term\">pytest</div><div class=\"def\">Standard for tests; functions starting with <code>test_</code></div></div>\n    <div class=\"fastcard\"><div class=\"term\">ruff / black</div><div class=\"def\">Formatter + linter; run on save</div></div>\n  </div>\n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>You don't need to \"learn Python\" — you need 5 areas: data structures, pandas, requests, DB drivers, CLI.</li>\n      <li>Pandas ≈ SQL with different syntax. Make a translation table cheat sheet.</li>\n      <li>PySpark = pandas-adjacent + lazy + distributed.</li>\n      <li>Patterns: idempotent scripts (date param + --full-refresh), retries with tenacity, logging not print.</li>\n    </ul>\n  </div>",
  "words": 364
 },
 {
  "id": "m3",
  "title": "Git, GitHub & CI/CD",
  "subtitle": "The single biggest \"looking like an engineer\" lever.",
  "html": "<p class=\"lead\">Analysts often live in notebooks or Tableau workbooks — no version control. AEs live in Git. The mental model: <em>every change is a branch → PR → review → merge</em>.</p>\n\n  <h3>The Git Mental Model</h3>\n  <table>\n    <tbody><tr><th>Concept</th><th>What it is</th></tr>\n    <tr><td>Repository</td><td>A folder + complete history of every change</td></tr>\n    <tr><td>Branch</td><td>An independent line of work. <code>main</code> = production. Feature branches off main.</td></tr>\n    <tr><td>Commit</td><td>A snapshot + message. Atomic — one logical change.</td></tr>\n    <tr><td>Push</td><td>Upload your branch to GitHub.</td></tr>\n    <tr><td>Pull Request</td><td>\"Please review and merge my branch.\"</td></tr>\n    <tr><td>Merge</td><td>Integrate one branch into another.</td></tr>\n    <tr><td>Rebase</td><td>Replay your commits on top of latest main. Cleaner history than merge.</td></tr>\n  </tbody></table>\n\n  <h3>The Daily Loop</h3>\n  <pre><code><span class=\"cm\"># start a new feature</span>\ngit checkout main\ngit pull origin main\ngit checkout -b feat/DAE-422-stg-accounts\n\n<span class=\"cm\"># work, then save it</span>\ngit add models/staging/stg_accounts.sql\ngit commit -m <span class=\"str\">\"feat: [DAE-422] Add staging model for accounts\"</span>\n\n<span class=\"cm\"># push and open a PR</span>\ngit push -u origin feat/DAE-422-stg-accounts\ngh pr create --title <span class=\"str\">\"feat: [DAE-422] stg_accounts\"</span> --body <span class=\"str\">\"...\"</span>\n\n<span class=\"cm\"># after review, merge via GitHub UI (squash merge)</span></code></pre>\n\n  <h3>Commit Hygiene</h3>\n  <ul>\n    <li><b>One logical change per commit.</b> \"Add stg_accounts\" not \"stg_accounts and some other fix and docs.\"</li>\n    <li><b>Conventional Commits</b>: <code>feat: …</code>, <code>fix: …</code>, <code>chore: …</code>, <code>refactor: …</code>, <code>docs: …</code></li>\n    <li><b>Imperative mood</b>: \"Add\", \"Fix\", \"Refactor\" — not \"Added\", \"Fixing\".</li>\n    <li><b>Reference ticket</b>: <code>feat: [DAE-422] Add staging model</code></li>\n  </ul>\n\n  <h3>Resolving Merge Conflicts</h3>\n  <pre><code><span class=\"cm\"># your branch is behind main — rebase to bring it up</span>\ngit fetch origin main\ngit rebase origin/main\n\n<span class=\"cm\"># conflict in a file — open it, look for &lt;&lt;&lt;&lt;&lt;&lt;&lt; markers</span>\n<span class=\"cm\"># fix it, then:</span>\ngit add &lt;file&gt;\ngit rebase --continue\n\n<span class=\"cm\"># abort if it's a mess</span>\ngit rebase --abort</code></pre>\n\n  <div class=\"callout danger\">\n    <div class=\"callout-title\">Never force-push to main</div>\n    <code>git push --force</code> on main rewrites everyone's history. Use it only on your own feature branches, and prefer <code>--force-with-lease</code>.\n  </div>\n\n  <h3>CI/CD for Data Projects</h3>\n  <p>CI = Continuous Integration. Every PR triggers automated checks: lint, compile, test, run dbt on a sample.</p>\n  <p>For a dbt project, a typical PR pipeline:</p>\n  <ol>\n    <li><b>Lint</b> — sqlfluff checks SQL style</li>\n    <li><b>Compile</b> — <code>dbt compile</code> catches syntax errors</li>\n    <li><b>Build modified</b> — <code>dbt build --select state:modified+</code> runs only changed models</li>\n    <li><b>Test</b> — schema + data tests run automatically as part of build</li>\n    <li><b>Slim CI</b> — only re-run what actually changed since main</li>\n  </ol>\n\n  <h3>Fast-Pointer Cards</h3>\n  <div class=\"fastcards\">\n    <div class=\"fastcard\"><div class=\"term\">git status</div><div class=\"def\">Show working tree state — run constantly</div></div>\n    <div class=\"fastcard\"><div class=\"term\">git diff</div><div class=\"def\">See unstaged changes; <code>--staged</code> for staged</div></div>\n    <div class=\"fastcard\"><div class=\"term\">git log --oneline</div><div class=\"def\">Compact history view</div></div>\n    <div class=\"fastcard\"><div class=\"term\">git stash</div><div class=\"def\">Temporarily shelve changes</div></div>\n    <div class=\"fastcard\"><div class=\"term\">git reset --soft HEAD~1</div><div class=\"def\">Undo last commit, keep changes staged</div></div>\n    <div class=\"fastcard\"><div class=\"term\">git cherry-pick</div><div class=\"def\">Apply a specific commit from elsewhere</div></div>\n    <div class=\"fastcard\"><div class=\"term\">gh pr view</div><div class=\"def\">CLI to read PR status</div></div>\n    <div class=\"fastcard\"><div class=\"term\">.gitignore</div><div class=\"def\">Files git should never track (creds, build artifacts)</div></div>\n  </div>\n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>Every change → branch → commit → push → PR → review → merge. No exceptions.</li>\n      <li>Conventional Commits + Jira reference: <code>feat: [DAE-422] description</code></li>\n      <li>Rebase to keep history linear; never force-push to main.</li>\n      <li>CI for dbt: lint, compile, build state:modified+, test.</li>\n    </ul>\n  </div>",
  "words": 456
 },
 {
  "id": "m4",
  "title": "Data Modeling",
  "subtitle": "The hardest, highest-leverage skill. This is where AE earns its title.",
  "html": "<p class=\"lead\">Modeling = deciding the <em>shape</em> of your tables: what's a fact, what's a dimension, what's the grain, how do they relate. Get this wrong and every dashboard is wrong forever.</p>\n\n  <h3>Normalization (3NF) vs Dimensional</h3>\n  <table>\n    <tbody><tr><th></th><th>3NF / Normalized</th><th>Dimensional (Kimball)</th></tr>\n    <tr><td>Where used</td><td>OLTP — transactional databases</td><td>OLAP — warehouses</td></tr>\n    <tr><td>Goal</td><td>No duplication, fast writes</td><td>Fast reads, intuitive queries</td></tr>\n    <tr><td>Shape</td><td>Many tables, lots of joins</td><td>Star schema — fact + dims</td></tr>\n    <tr><td>Example</td><td>production MySQL DBs</td><td>your dbt marts/ layer</td></tr>\n  </tbody></table>\n\n  <h3>Kimball Star Schema — the AE bread &amp; butter</h3>\n  <p>A <b>fact</b> is something that happened (a transaction, an event). A <b>dimension</b> describes the actors (who, what, where, when).</p>\n\n  <div class=\"two-col\">\n    <div class=\"card\">\n      <h4>Fact table</h4>\n      <ul>\n        <li>One row = one event (1 booking, 1 job posted)</li>\n        <li>Mostly foreign keys + numerical measures</li>\n        <li>Grain stated explicitly (e.g. \"one row per job posted\")</li>\n        <li>Examples: <code>fct_jobs</code>, <code>fct_bookings</code>, <code>fct_payments</code></li>\n      </ul>\n    </div>\n    <div class=\"card\">\n      <h4>Dimension table</h4>\n      <ul>\n        <li>One row = one entity (1 account, 1 contractor, 1 product)</li>\n        <li>Mostly descriptive attributes</li>\n        <li>Slowly changes over time (SCDs)</li>\n        <li>Examples: <code>dim_accounts</code>, <code>dim_contractors</code>, <code>dim_dates</code></li>\n      </ul>\n    </div>\n  </div>\n\n  <h3>The Grain — what one row represents</h3>\n  <p>Always declare it. Write it in the model docs. Every join, every aggregate either preserves or changes the grain. Confusion about grain = duplicated rows = wrong metrics.</p>\n  <pre><code><span class=\"cm\">-- fct_job_assignments grain: one row per (job_id, contractor_id)\n-- fct_jobs            grain: one row per job_id\n-- fct_daily_revenue   grain: one row per (account_id, date_day)</span></code></pre>\n\n  <h3>Slowly Changing Dimensions (SCDs)</h3>\n  <table>\n    <tbody><tr><th>Type</th><th>Behavior</th><th>Use case</th></tr>\n    <tr><td>Type 0</td><td>Never change</td><td>Birth date</td></tr>\n    <tr><td><b>Type 1</b></td><td>Overwrite — only current value</td><td>Customer email (don't care about history)</td></tr>\n    <tr><td><b>Type 2</b></td><td>Keep history with <code>valid_from</code> / <code>valid_to</code> / <code>is_current</code></td><td>Plan tier, sales rep — most common in AE</td></tr>\n    <tr><td>Type 3</td><td>Add column for \"previous value\"</td><td>Rare; limited history</td></tr>\n    <tr><td>Type 4</td><td>Separate history table</td><td>Heavy-history dims</td></tr>\n    <tr><td>Type 6</td><td>1+2+3 hybrid</td><td>Edge cases</td></tr>\n  </tbody></table>\n\n  <h4>SCD Type 2 in dbt — snapshots</h4>\n  <pre><code><span class=\"cm\">-- snapshots/accounts_snapshot.sql</span>\n<span class=\"fn\">{% snapshot accounts_snapshot %}</span>\n<span class=\"fn\">{{ config(</span>\n    target_schema=<span class=\"str\">'snapshots'</span>,\n    unique_key=<span class=\"str\">'account_id'</span>,\n    strategy=<span class=\"str\">'timestamp'</span>,\n    updated_at=<span class=\"str\">'updated_at'</span>\n<span class=\"fn\">) }}</span>\n<span class=\"kw\">select</span> * <span class=\"kw\">from</span> <span class=\"fn\">{{ source('raw', 'accounts') }}</span>\n<span class=\"fn\">{% endsnapshot %}</span></code></pre>\n  <p>This builds a table with <code>dbt_valid_from</code> and <code>dbt_valid_to</code> columns — every change to <code>updated_at</code> creates a new row.</p>\n\n  <h3>Surrogate vs Natural Keys</h3>\n  <ul>\n    <li><b>Natural key</b> — comes from source: <code>email</code>, <code>job_number</code>. Can change.</li>\n    <li><b>Surrogate key</b> — generated, immutable: <code>account_sk = md5(account_id || updated_at)</code>.</li>\n    <li>For SCD2 dims: surrogate key must include the time component, otherwise joins break.</li>\n    <li>dbt: <code>{{ dbt_utils.generate_surrogate_key(['col_a','col_b']) }}</code></li>\n  </ul>\n  <div class=\"callout warn\">\n    <div class=\"callout-title\">Production gotcha</div>\n    <code>generate_surrogate_key</code> with a nullable column gives identical hashes for different NULL rows. Always use a column that's unique-per-row.\n  </div>\n\n  <h3>The Three-Layer Pattern (a common dbt convention)</h3>\n  <table>\n    <tbody><tr><th>Layer</th><th>Purpose</th><th>Examples</th></tr>\n    <tr><td><b>staging/</b></td><td>Light cleaning + rename. 1:1 with source.</td><td><code>stg_accounts</code>, <code>stg_jobs</code></td></tr>\n    <tr><td><b>intermediate/</b></td><td>Business logic, joins, fan-out resolution.</td><td><code>int_account_jobs_joined</code></td></tr>\n    <tr><td><b>marts/</b></td><td>Final fact/dim tables exposed to BI.</td><td><code>fct_jobs</code>, <code>dim_accounts</code></td></tr>\n  </tbody></table>\n\n  <h3>Medallion Architecture (Databricks/Lakehouse term)</h3>\n  <p>Same idea, different naming:</p>\n  <ul>\n    <li><b>Bronze</b> — raw ingested data, minimal transformation. Mirrors source.</li>\n    <li><b>Silver</b> — cleaned, conformed, joined. Roughly = staging + intermediate.</li>\n    <li><b>Gold</b> — business-ready aggregates. Roughly = marts.</li>\n  </ul>\n\n  <h3>Modeling Anti-Patterns</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>The \"one big table\"</h4>\n      <p>Dumping every column from every source into <code>fct_everything</code>. Looks easy, becomes unmaintainable. Resist.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Mixing grains</h4>\n      <p>Joining <code>fct_jobs</code> (one row per job) to <code>fct_job_events</code> (many per job) without aggregating first. Fan-out everywhere.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Wide skinny dim</h4>\n      <p>Dimension with 200 columns, 90% nulls. Split into role-playing or junk dimensions.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Forgetting the date dim</h4>\n      <p>Always have <code>dim_dates</code>. Lets you reason about fiscal weeks, holidays, business days — without case statements.</p>\n    </div>\n  </div>\n\n  <h3>Flashcards</h3>\n  <div class=\"flashcard-set\">\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">What's the grain of a fact table and why does it matter?</div>\n      <div class=\"answer\">Grain = what one row represents (e.g. \"one row per job per day\"). It's the contract for the table — every metric is implicitly per-grain. Mixing grains in joins causes silent fan-out and wrong sums.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">When do you use SCD Type 2 vs Type 1?</div>\n      <div class=\"answer\">Type 2 when historical accuracy matters (was this account on \"Pro\" plan when this lead was sold?). Type 1 when you only ever need the current value (their current email address).</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">Star schema vs snowflake schema?</div>\n      <div class=\"answer\">Star: dims are denormalized — fewer joins, easier for BI. Snowflake: dims are normalized into sub-dims (e.g. product → category → department) — more joins, less redundancy. Star wins in most modern warehouses.</div>\n    </div>\n  </div>\n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>Star schema = fact (events) + dim (entities). Most warehouses live here.</li>\n      <li>Declare the grain of every fact table. Honor it in every join.</li>\n      <li>SCD Type 2 for historical accuracy; Type 1 for \"only current\" use cases.</li>\n      <li>Three-layer dbt pattern: staging → intermediate → marts (= bronze/silver/gold).</li>\n      <li>Always have a <code>dim_dates</code>. Anti-pattern: one big table, mixing grains.</li>\n    </ul>\n  </div>",
  "words": 697
 },
 {
  "id": "m5",
  "title": "Data Warehousing Concepts",
  "subtitle": "Why your query is slow — and how to make it 100× faster.",
  "html": "<p class=\"lead\">Modern warehouses (Databricks, Snowflake, BigQuery) are columnar, distributed, and lazy. Knowing what they actually do under the hood is the difference between a 5-second query and a 5-minute one — at the same cost.</p>\n\n  <h3>OLTP vs OLAP</h3>\n  <table>\n    <tbody><tr><th></th><th>OLTP (transactional)</th><th>OLAP (analytical)</th></tr>\n    <tr><td>Example</td><td>production MySQL</td><td>Databricks, Snowflake, BigQuery</td></tr>\n    <tr><td>Workload</td><td>Many tiny writes/reads</td><td>Few large scans / aggregations</td></tr>\n    <tr><td>Storage</td><td>Row-oriented</td><td>Column-oriented</td></tr>\n    <tr><td>Optimised for</td><td>Latency on single row</td><td>Throughput on millions of rows</td></tr>\n  </tbody></table>\n\n  <h3>Columnar storage — the OLAP secret</h3>\n  <p>Row-store writes a row's fields together: <code>[id1, name1, age1] [id2, name2, age2] …</code></p>\n  <p>Column-store writes each column together: <code>[id1, id2, …] [name1, name2, …] [age1, age2, …]</code></p>\n  <p><b>Implication:</b> <code>SELECT AVG(age)</code> reads ONLY the age column. <code>SELECT *</code> reads everything. <em>This is why <code>SELECT *</code> is expensive in OLAP and basically free in OLTP.</em></p>\n\n  <h3>File formats</h3>\n  <table>\n    <tbody><tr><th>Format</th><th>Type</th><th>Use case</th></tr>\n    <tr><td>CSV</td><td>Row, text</td><td>Interchange, never analytical storage</td></tr>\n    <tr><td>JSON</td><td>Row, nested</td><td>API responses, semi-structured raw</td></tr>\n    <tr><td><b>Parquet</b></td><td>Columnar, compressed</td><td>The standard for warehouse storage</td></tr>\n    <tr><td>ORC</td><td>Columnar, compressed</td><td>Hive ecosystem</td></tr>\n    <tr><td>Avro</td><td>Row, schema'd</td><td>Streaming (Kafka payloads)</td></tr>\n    <tr><td><b>Delta / Iceberg / Hudi</b></td><td>Parquet + transaction log</td><td>Lakehouse table formats — ACID on object storage</td></tr>\n  </tbody></table>\n\n  <h3>Delta Lake — the lakehouse table format</h3>\n  <p>Delta = Parquet files + a <code>_delta_log/</code> JSON transaction log. The log records every commit (add files, remove files, schema change). This gives you:</p>\n  <ul>\n    <li><b>ACID transactions</b> on cheap object storage (S3/ADLS/GCS).</li>\n    <li><b>Time travel</b>: <code>SELECT * FROM t VERSION AS OF 12</code></li>\n    <li><b>Schema enforcement &amp; evolution</b></li>\n    <li><b>MERGE INTO</b> — upserts (the CDC backbone)</li>\n    <li><b>OPTIMIZE</b> + <b>ZORDER</b> — small-file compaction + clustering</li>\n  </ul>\n\n  <h3>Partitioning</h3>\n  <p>Physically splits a table by a column value. Queries that filter on that column skip entire partitions.</p>\n  <pre><code><span class=\"cm\">-- partitioned by date_day → query only scans 1 day</span>\n<span class=\"kw\">create table</span> events (\n  event_id <span class=\"kw\">bigint</span>,\n  user_id <span class=\"kw\">bigint</span>,\n  date_day <span class=\"kw\">date</span>,\n  ...\n)\n<span class=\"kw\">using delta</span>\n<span class=\"kw\">partitioned by</span> (date_day);</code></pre>\n  <div class=\"callout warn\">\n    <div class=\"callout-title\">Don't over-partition</div>\n    Partition cardinality should be moderate (10s–1000s). Partitioning by <code>user_id</code> with 10M users = 10M tiny files = disaster.\n  </div>\n\n  <h3>Clustering / Z-Ordering</h3>\n  <p>Within a partition (or whole table), sort the physical files by a column so scans can skip files via min/max stats.</p>\n  <pre><code><span class=\"kw\">optimize</span> events <span class=\"kw\">zorder by</span> (user_id);</code></pre>\n  <p>Use Z-order on high-cardinality columns frequently filtered on. Liquid Clustering (newer) replaces Z-order — pick this on new Delta tables.</p>\n\n  <h3>The 4 Performance Levers</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>1. Partition pruning</h4>\n      <p>Filter on partition column. Skip whole folders.</p>\n    </div>\n    <div class=\"card\">\n      <h4>2. Predicate pushdown</h4>\n      <p>Filter before any join. Engine reads less Parquet.</p>\n    </div>\n    <div class=\"card\">\n      <h4>3. Column projection</h4>\n      <p>SELECT only what you need. Columnar storage rewards this.</p>\n    </div>\n    <div class=\"card\">\n      <h4>4. Broadcast joins</h4>\n      <p>Small dim broadcast to all workers — no shuffle. <code>/*+ BROADCAST(dim) */</code></p>\n    </div>\n  </div>\n\n  <h3>Compute Models</h3>\n  <table>\n    <tbody><tr><th></th><th>Description</th><th>Examples</th></tr>\n    <tr><td>Storage = Compute</td><td>Tied — bigger table needs bigger box</td><td>Old Postgres data warehouses</td></tr>\n    <tr><td><b>Storage ≠ Compute</b></td><td>Decoupled — scale them independently</td><td>Databricks, Snowflake, BigQuery</td></tr>\n    <tr><td>Serverless</td><td>Auto-scale, pay per query</td><td>Databricks SQL Serverless, BigQuery</td></tr>\n  </tbody></table>\n\n  <h3>Costs — the new AE concern</h3>\n  <ul>\n    <li>You're billed for compute time, not data volume scanned (mostly). Bigger warehouse = faster but more $/sec.</li>\n    <li>Rule of thumb: small warehouse for dev/sandbox; right-size production by p95 query latency.</li>\n    <li>Common waste: <code>SELECT *</code> in dashboards, queries running 24/7 on a giant warehouse, no auto-stop.</li>\n  </ul>\n\n  <h3>Flashcards</h3>\n  <div class=\"flashcard-set\">\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">Why is <code>SELECT *</code> bad in OLAP but fine in OLTP?</div>\n      <div class=\"answer\">Columnar storage reads only requested columns. SELECT * forces every column off disk — often 10–50× more I/O than needed. OLTP is row-oriented so it costs the same regardless.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">What does <code>OPTIMIZE … ZORDER BY (col)</code> do?</div>\n      <div class=\"answer\">Compacts small Parquet files into bigger ones AND sorts data by <code>col</code> so engines can skip files via min/max stats during filtered queries. Speeds up filters on high-cardinality columns.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">Why does Delta Lake give you ACID on S3/cloud storage?</div>\n      <div class=\"answer\">A transaction log (<code>_delta_log/</code>) ordered as a sequence of JSON commits. Atomic file rename guarantees serializability. Without it, S3 is \"eventually consistent\" and you can't do upserts safely.</div>\n    </div>\n  </div>\n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>OLAP = columnar + distributed + decoupled storage/compute.</li>\n      <li>Parquet is the standard format; Delta/Iceberg add ACID + time travel on top.</li>\n      <li>4 levers: partition pruning, predicate pushdown, projection, broadcast joins.</li>\n      <li>Don't over-partition. Z-order/Liquid Clustering for high-cardinality filters.</li>\n      <li>Cost = compute time. Right-size warehouse; SELECT only what you need.</li>\n    </ul>\n  </div>",
  "words": 645
 },
 {
  "id": "m6",
  "title": "dbt Mastery",
  "subtitle": "The framework that defined Analytics Engineering.",
  "html": "<p class=\"lead\">dbt is the tool, but more importantly, it's the workflow. SELECT statements → version-controlled, tested, documented, lineage-aware models. If you can ship dbt comfortably, you can call yourself an AE.</p>\n\n  <h3>The dbt Mental Model</h3>\n  <ol>\n    <li>You write SELECT statements in <code>.sql</code> files inside <code>models/</code>.</li>\n    <li>dbt wraps each one in <code>CREATE TABLE AS</code> or <code>CREATE VIEW AS</code>.</li>\n    <li>You reference other models with <code>{{ ref('model_name') }}</code> — dbt builds the DAG.</li>\n    <li><code>dbt run</code> executes them in dependency order.</li>\n    <li><code>dbt test</code> runs assertions you've declared.</li>\n    <li><code>dbt build</code> = run + test together.</li>\n  </ol>\n\n  <h3>Project Anatomy</h3>\n  <pre><code>my_dbt_project/\n├── dbt_project.yml         <span class=\"cm\"># project config</span>\n├── profiles.yml             <span class=\"cm\"># warehouse connection</span>\n├── models/\n│   ├── staging/\n│   │   ├── _sources.yml     <span class=\"cm\"># declare raw tables</span>\n│   │   ├── _models.yml      <span class=\"cm\"># tests + docs</span>\n│   │   └── stg_accounts.sql\n│   ├── intermediate/\n│   │   └── int_account_jobs.sql\n│   └── marts/\n│       ├── fct_jobs.sql\n│       └── dim_accounts.sql\n├── tests/                   <span class=\"cm\"># custom data tests</span>\n├── macros/                  <span class=\"cm\"># reusable Jinja</span>\n├── snapshots/               <span class=\"cm\"># SCD2 tracking</span>\n└── seeds/                   <span class=\"cm\"># static CSVs to load</span></code></pre>\n\n  <h3>Materializations</h3>\n  <table>\n    <tbody><tr><th>Type</th><th>What it builds</th><th>When to use</th></tr>\n    <tr><td><b>view</b></td><td>View — runs underlying SQL every time</td><td>Tiny lookup, no perf concern</td></tr>\n    <tr><td><b>table</b></td><td>Full table — recreated each run</td><td>Default for most marts</td></tr>\n    <tr><td><b>incremental</b></td><td>Only new/changed rows appended each run</td><td>Big fact tables, append-only events</td></tr>\n    <tr><td><b>ephemeral</b></td><td>Inlined CTE in downstream models</td><td>Reusable logic, no physical table</td></tr>\n    <tr><td><b>snapshot</b></td><td>Type 2 SCD with valid_from/to</td><td>Track historical changes</td></tr>\n    <tr><td><b>materialized_view</b></td><td>Auto-refreshed by warehouse (newer)</td><td>Near-real-time aggregates</td></tr>\n  </tbody></table>\n\n  <h3>Incremental Models — the most-asked AE topic</h3>\n  <pre><code><span class=\"cm\">-- models/marts/fct_events.sql</span>\n<span class=\"fn\">{{ config(</span>\n    materialized=<span class=\"str\">'incremental'</span>,\n    unique_key=<span class=\"str\">'event_id'</span>,\n    on_schema_change=<span class=\"str\">'sync_all_columns'</span>,\n    incremental_strategy=<span class=\"str\">'merge'</span>\n<span class=\"fn\">) }}</span>\n\n<span class=\"kw\">select</span> *\n<span class=\"kw\">from</span> <span class=\"fn\">{{ ref('stg_events') }}</span>\n<span class=\"fn\">{% if is_incremental() %}</span>\n  <span class=\"kw\">where</span> updated_at &gt; (<span class=\"kw\">select</span> <span class=\"fn\">max</span>(updated_at) <span class=\"kw\">from</span> <span class=\"fn\">{{ this }}</span>)\n<span class=\"fn\">{% endif %}</span></code></pre>\n  <p>On first run, no filter → full build. After that, only new rows. <code>unique_key + merge strategy</code> updates existing rows where the key matches.</p>\n\n  <div class=\"callout warn\">\n    <div class=\"callout-title\">Incremental gotchas</div>\n    <ul>\n      <li>Late-arriving data: filter on <code>updated_at &gt; max - 1 hour</code> for safety margin.</li>\n      <li>Schema changes: set <code>on_schema_change</code> or you'll get errors.</li>\n      <li>Always rebuildable: <code>dbt run --full-refresh</code> drops and rebuilds.</li>\n      <li>In a CDC pipeline: first run after adding a CDC checkpoint column may need <code>--full-refresh</code>.</li>\n    </ul>\n  </div>\n\n  <h3>Tests — the safety net</h3>\n  <p>dbt ships with 4 generic tests built-in:</p>\n  <ul>\n    <li><code>unique</code> — column has no duplicates</li>\n    <li><code>not_null</code> — column has no NULLs</li>\n    <li><code>accepted_values</code> — column ∈ set</li>\n    <li><code>relationships</code> — every value exists in another table's column (FK check)</li>\n  </ul>\n  <pre><code><span class=\"cm\"># models/marts/_models.yml</span>\n<span class=\"kw\">version:</span> 2\n<span class=\"kw\">models:</span>\n  - <span class=\"kw\">name:</span> fct_jobs\n    <span class=\"kw\">columns:</span>\n      - <span class=\"kw\">name:</span> job_id\n        <span class=\"kw\">tests:</span>\n          - unique\n          - not_null\n      - <span class=\"kw\">name:</span> account_id\n        <span class=\"kw\">tests:</span>\n          - relationships:\n              to: ref(<span class=\"str\">'dim_accounts'</span>)\n              field: account_id\n      - <span class=\"kw\">name:</span> status\n        <span class=\"kw\">tests:</span>\n          - accepted_values:\n              values: [<span class=\"str\">'open'</span>, <span class=\"str\">'closed'</span>, <span class=\"str\">'cancelled'</span>]</code></pre>\n\n  <h4>dbt_utils + dbt-expectations</h4>\n  <p>Add packages for ~50 more tests: <code>expression_is_true</code>, <code>row_count_delta</code>, <code>not_null_proportion</code>, <code>recency</code>, etc.</p>\n\n  <h3>Macros &amp; Jinja — reuse without copy-paste</h3>\n  <pre><code><span class=\"cm\">-- macros/audit_columns.sql</span>\n<span class=\"fn\">{% macro audit_columns() %}</span>\n  current_timestamp() <span class=\"kw\">as</span> dbt_loaded_at,\n  <span class=\"str\">'{{ invocation_id }}'</span> <span class=\"kw\">as</span> dbt_run_id,\n  <span class=\"str\">'{{ this.identifier }}'</span> <span class=\"kw\">as</span> dbt_model_name\n<span class=\"fn\">{% endmacro %}</span>\n\n<span class=\"cm\">-- used in any model:</span>\n<span class=\"kw\">select</span>\n  *,\n  <span class=\"fn\">{{ audit_columns() }}</span>\n<span class=\"kw\">from</span> <span class=\"fn\">{{ ref('stg_accounts') }}</span></code></pre>\n  <div class=\"callout\">\n    <div class=\"callout-title\">A common convention</div>\n    Every dbt model ends with <code>{{ audit_columns() }}</code>. Captures load time + run id for debugging.\n  </div>\n\n  <h3>Sources, Refs, and Lineage</h3>\n  <ul>\n    <li><b>source</b>: a raw table dbt didn't create. Declared in <code>_sources.yml</code>. Reference via <code>{{ source('raw', 'accounts') }}</code>.</li>\n    <li><b>ref</b>: another dbt model. Reference via <code>{{ ref('stg_accounts') }}</code>.</li>\n    <li>dbt parses these to build the DAG. <code>dbt docs generate &amp;&amp; dbt docs serve</code> gives you a visual lineage browser.</li>\n  </ul>\n\n  <h3>Selectors — control what runs</h3>\n  <table>\n    <tbody><tr><th>Syntax</th><th>Meaning</th></tr>\n    <tr><td><code>dbt build -s fct_jobs</code></td><td>Just this model</td></tr>\n    <tr><td><code>dbt build -s +fct_jobs</code></td><td>fct_jobs + all upstream</td></tr>\n    <tr><td><code>dbt build -s fct_jobs+</code></td><td>fct_jobs + all downstream</td></tr>\n    <tr><td><code>dbt build -s +fct_jobs+</code></td><td>fct_jobs + upstream + downstream</td></tr>\n    <tr><td><code>dbt build -s tag:daily</code></td><td>All models tagged 'daily'</td></tr>\n    <tr><td><code>dbt build -s state:modified+</code></td><td>Changed since main, plus downstream — CI gold</td></tr>\n  </tbody></table>\n\n  <h3>Snapshots (SCD2)</h3>\n  <pre><code><span class=\"fn\">{% snapshot accounts_snapshot %}</span>\n<span class=\"fn\">{{ config(</span>\n    target_schema=<span class=\"str\">'snapshots'</span>,\n    unique_key=<span class=\"str\">'account_id'</span>,\n    strategy=<span class=\"str\">'check'</span>,\n    check_cols=[<span class=\"str\">'plan_tier'</span>, <span class=\"str\">'status'</span>]\n<span class=\"fn\">) }}</span>\n<span class=\"kw\">select</span> account_id, plan_tier, status\n<span class=\"kw\">from</span> <span class=\"fn\">{{ source('raw', 'accounts') }}</span>\n<span class=\"fn\">{% endsnapshot %}</span></code></pre>\n  <p>Strategies: <code>timestamp</code> (compare updated_at column) or <code>check</code> (compare specific columns hash). Adds <code>dbt_valid_from</code>, <code>dbt_valid_to</code>, <code>dbt_scd_id</code>.</p>\n\n  <h3>Exposures + Documentation</h3>\n  <p>Declare BI dashboards/reports that depend on your models — they appear in lineage.</p>\n  <pre><code><span class=\"cm\"># models/marts/_exposures.yml</span>\n<span class=\"kw\">exposures:</span>\n  - <span class=\"kw\">name:</span> sales_dashboard\n    <span class=\"kw\">type:</span> dashboard\n    <span class=\"kw\">owner:</span>\n      email: sales-ops@example.com\n    <span class=\"kw\">depends_on:</span>\n      - ref(<span class=\"str\">'fct_jobs'</span>)\n      - ref(<span class=\"str\">'dim_accounts'</span>)</code></pre>\n\n  <h3>Daily dbt Commands</h3>\n  <div class=\"cheatsheet\">\n<span class=\"label\">dbt run</span>                          <span class=\"comment\"># build all models</span>\n<span class=\"label\">dbt test</span>                         <span class=\"comment\"># run all tests</span>\n<span class=\"label\">dbt build</span>                        <span class=\"comment\"># run + test in DAG order (preferred)</span>\n<span class=\"label\">dbt build -s +fct_jobs</span>           <span class=\"comment\"># this model + all upstream</span>\n<span class=\"label\">dbt build -s state:modified+</span>    <span class=\"comment\"># CI: changed + downstream</span>\n<span class=\"label\">dbt run --full-refresh -s fct_jobs</span> <span class=\"comment\"># drop &amp; rebuild incremental</span>\n<span class=\"label\">dbt compile</span>                      <span class=\"comment\"># compile to SQL, don't run</span>\n<span class=\"label\">dbt docs generate &amp;&amp; dbt docs serve</span> <span class=\"comment\"># lineage UI</span>\n<span class=\"label\">dbt snapshot</span>                     <span class=\"comment\"># build snapshots</span>\n<span class=\"label\">dbt seed</span>                         <span class=\"comment\"># load CSVs from seeds/</span>\n<span class=\"label\">dbt clean</span>                        <span class=\"comment\"># nuke target/ and dbt_packages/</span>\n  </div>\n\n  <h3>Flashcards</h3>\n  <div class=\"flashcard-set\">\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">What does <code>{{ ref('stg_accounts') }}</code> compile to?</div>\n      <div class=\"answer\">The fully-qualified table or view name in the target schema (e.g. <code>analytics_dev.stg_accounts</code>). dbt rewrites refs based on the current target.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">When is an incremental model the wrong choice?</div>\n      <div class=\"answer\">When data backfills retroactively change old rows (rebuild logic doesn't catch them), when the table is small (overhead not worth it), or when you can't reliably detect \"new rows\" via a high-watermark column.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">Difference between sources and refs?</div>\n      <div class=\"answer\"><code>source</code> = raw table from upstream system (not built by dbt). <code>ref</code> = another dbt model. Both contribute to lineage; source freshness can be tested.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">What's the <code>relationships</code> test?</div>\n      <div class=\"answer\">Asserts every value in column A exists in column B of another model — essentially a referential integrity / FK check.</div>\n    </div>\n  </div>\n\n  \n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>dbt = SELECT → table/view + DAG + tests + docs + lineage.</li>\n      <li>Materializations: view / table / incremental / ephemeral / snapshot.</li>\n      <li>Incremental needs <code>unique_key</code> + a high-watermark filter + <code>on_schema_change</code>.</li>\n      <li>4 built-in tests; expand with dbt_utils + dbt_expectations.</li>\n      <li>Slim CI: <code>dbt build -s state:modified+</code>.</li>\n      <li>Convention: every model ends with <code>{{ audit_columns() }}</code>.</li>\n    </ul>\n  </div>",
  "words": 933
 },
 {
  "id": "m7",
  "title": "Databricks & Spark",
  "subtitle": "The platform you're already using — but probably under-using.",
  "html": "<p class=\"lead\">Spark is the engine; Databricks is the managed product around it. As an AE, knowing how Spark thinks about your query is a force multiplier — you'll diagnose 10x slowdowns yourself instead of escalating.</p>\n\n  <h3>Spark Architecture (one-paragraph version)</h3>\n  <p>A <b>Driver</b> coordinates. Multiple <b>Executors</b> on worker nodes run tasks in parallel. Data is partitioned across executors. The driver builds a <b>DAG</b> of stages from your code, then schedules tasks. <b>Shuffles</b> (data movement between executors) are the expensive operation.</p>\n\n  <h3>The Spark Vocabulary</h3>\n  <table>\n    <tbody><tr><th>Term</th><th>Meaning</th></tr>\n    <tr><td><b>Transformation</b></td><td>Lazy op (filter, groupBy, join). Builds plan, doesn't execute.</td></tr>\n    <tr><td><b>Action</b></td><td>Triggers execution (show, collect, count, write).</td></tr>\n    <tr><td><b>Partition</b></td><td>A chunk of data on one executor.</td></tr>\n    <tr><td><b>Stage</b></td><td>A group of tasks that can run without a shuffle in between.</td></tr>\n    <tr><td><b>Shuffle</b></td><td>Repartitioning data across nodes — expensive.</td></tr>\n    <tr><td><b>Job</b></td><td>One action = one job.</td></tr>\n    <tr><td><b>AQE</b></td><td>Adaptive Query Execution — runtime re-optimisation.</td></tr>\n    <tr><td><b>Photon</b></td><td>Databricks' vectorised native execution engine. Free speed.</td></tr>\n  </tbody></table>\n\n  <h3>Join Strategies</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>Broadcast Hash Join</h4>\n      <p>Small side broadcast to all executors. No shuffle. <b>Fastest</b>.<br>Triggers if smaller side &lt; <code>autoBroadcastJoinThreshold</code> (default 10MB).</p>\n    </div>\n    <div class=\"card\">\n      <h4>Sort-Merge Join</h4>\n      <p>Both sides shuffled by key, sorted, merged. Default for big-vs-big.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Shuffle Hash Join</h4>\n      <p>Less common; both shuffled, hash table built on smaller.</p>\n    </div>\n    <div class=\"card\">\n      <h4>BNLJ (Cartesian)</h4>\n      <p>Broadcast nested loop. The \"you broke something\" join — usually a missing join key.</p>\n    </div>\n  </div>\n\n  <h3>Reading a Spark UI / Query Profile</h3>\n  <ol>\n    <li><b>Jobs tab</b> — find the slow job, click in.</li>\n    <li><b>Stages tab</b> — look for a stage with high duration or high shuffle read/write.</li>\n    <li><b>Tasks</b> — check for <b>skew</b>: one task taking 10× the rest = skewed partition key.</li>\n    <li><b>SQL/DataFrame tab</b> — see the physical plan. Look for <code>SortMergeJoin</code> where you expected <code>BroadcastHashJoin</code> → missing broadcast hint or stale stats.</li>\n  </ol>\n\n  <h3>Common Spark Performance Issues</h3>\n  <table>\n    <tbody><tr><th>Symptom</th><th>Likely cause</th><th>Fix</th></tr>\n    <tr><td>One task takes 10× longer than others</td><td>Data skew (one key has 90% of rows)</td><td>Salting, AQE skew handling, broadcast if other side is small</td></tr>\n    <tr><td>Many tiny output files</td><td>Too many partitions → too many writers</td><td><code>coalesce(n)</code> or <code>OPTIMIZE</code> after</td></tr>\n    <tr><td>Job takes ages on small data</td><td>Cluster spin-up overhead</td><td>Use serverless or warm cluster pool</td></tr>\n    <tr><td>SortMergeJoin where BHJ expected</td><td>Stats stale or table too big</td><td>ANALYZE TABLE; explicit <code>/*+ BROADCAST(t) */</code></td></tr>\n    <tr><td>Driver OOM</td><td><code>.collect()</code> on huge DataFrame</td><td>Never collect big data to driver</td></tr>\n  </tbody></table>\n\n  <h3>Databricks Platform Concepts</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>Unity Catalog</h4>\n      <p>Governance layer: 3-level namespace <code>catalog.schema.table</code>, RBAC, lineage, tags. The successor to Hive metastore.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Delta Lake</h4>\n      <p>Default table format. ACID, time travel, MERGE, OPTIMIZE, vacuum.</p>\n    </div>\n    <div class=\"card\">\n      <h4>DBSQL Warehouses</h4>\n      <p>Serverless SQL endpoints for BI/dbt. Sizing: 2X-Small → 4X-Large. Auto-stop is critical.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Workflows</h4>\n      <p>Databricks' orchestrator. Job = DAG of tasks (notebooks/SQL/dbt/Python).</p>\n    </div>\n    <div class=\"card\">\n      <h4>AI/BI Dashboards</h4>\n      <p>The Lakeview successor. SQL datasets + widgets. JSON-defined, deployable via CLI.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Genie</h4>\n      <p>Natural-language BI layer; configured via instructions, sample queries, joins.</p>\n    </div>\n  </div>\n\n  <h3>PySpark Patterns You'll Actually Use</h3>\n  <pre><code><span class=\"kw\">from</span> pyspark.sql <span class=\"kw\">import</span> functions <span class=\"kw\">as</span> F, Window\n\n<span class=\"cm\"># Read Delta</span>\ndf = spark.read.format(<span class=\"str\">\"delta\"</span>).table(<span class=\"str\">\"main.silver.jobs\"</span>)\n\n<span class=\"cm\"># Filter + project (predicate pushdown happens automatically)</span>\ndf = df.filter(F.col(<span class=\"str\">\"is_deleted\"</span>) == <span class=\"kw\">False</span>) \\\n       .select(<span class=\"str\">\"job_id\"</span>, <span class=\"str\">\"account_id\"</span>, <span class=\"str\">\"created_at\"</span>)\n\n<span class=\"cm\"># Dedupe latest per key</span>\nw = Window.partitionBy(<span class=\"str\">\"job_id\"</span>).orderBy(F.col(<span class=\"str\">\"updated_at\"</span>).desc())\ndf = df.withColumn(<span class=\"str\">\"rn\"</span>, F.row_number().over(w)).filter(<span class=\"str\">\"rn = 1\"</span>).drop(<span class=\"str\">\"rn\"</span>)\n\n<span class=\"cm\"># Join with explicit broadcast</span>\ndims = spark.read.table(<span class=\"str\">\"main.silver.dim_accounts\"</span>)\njoined = df.join(F.broadcast(dims), <span class=\"str\">\"account_id\"</span>, <span class=\"str\">\"left\"</span>)\n\n<span class=\"cm\"># Write to Delta with merge (upsert)</span>\n<span class=\"kw\">from</span> delta.tables <span class=\"kw\">import</span> DeltaTable\ntgt = DeltaTable.forName(spark, <span class=\"str\">\"main.gold.fct_jobs\"</span>)\ntgt.alias(<span class=\"str\">\"t\"</span>).merge(\n    df.alias(<span class=\"str\">\"s\"</span>), <span class=\"str\">\"t.job_id = s.job_id\"</span>\n).whenMatchedUpdateAll().whenNotMatchedInsertAll().execute()</code></pre>\n\n  <h3>Delta Maintenance — the boring critical stuff</h3>\n  <pre><code><span class=\"cm\">-- compact small files + sort by access patterns</span>\n<span class=\"kw\">optimize</span> main.gold.fct_jobs <span class=\"kw\">zorder by</span> (account_id, event_date);\n\n<span class=\"cm\">-- liquid clustering (newer, preferred for new tables)</span>\n<span class=\"kw\">alter table</span> fct_jobs <span class=\"kw\">cluster by</span> (account_id, event_date);\n\n<span class=\"cm\">-- remove old files older than retention period</span>\n<span class=\"kw\">vacuum</span> main.gold.fct_jobs <span class=\"kw\">retain</span> <span class=\"num\">168</span> <span class=\"kw\">hours</span>;\n\n<span class=\"cm\">-- time travel for debugging</span>\n<span class=\"kw\">select</span> * <span class=\"kw\">from</span> fct_jobs <span class=\"kw\">version as of</span> <span class=\"num\">42</span>;\n<span class=\"kw\">select</span> * <span class=\"kw\">from</span> fct_jobs <span class=\"kw\">timestamp as of</span> <span class=\"str\">'2026-05-01 00:00:00'</span>;</code></pre>\n\n  <h3>Flashcards</h3>\n  <div class=\"flashcard-set\">\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">Why is a broadcast hash join faster than a sort-merge join?</div>\n      <div class=\"answer\">No shuffle. The small side is sent to every executor once; each executor joins locally. Sort-merge shuffles both sides across the network and sorts them — expensive.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">You see one task in a stage taking 20× longer than the others. What's likely?</div>\n      <div class=\"answer\">Data skew — one key in the join/groupBy has dramatically more rows. Fix: AQE skew handling, salting, or broadcast the smaller side.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">What does <code>VACUUM</code> on a Delta table do? When NOT to run it?</div>\n      <div class=\"answer\">Removes data files older than the retention period (default 7 days) that are no longer referenced. Don't run with a short retention if you rely on time travel — you'll lose history.</div>\n    </div>\n  </div>\n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>Driver schedules, executors run, shuffles cost. Photon = free speedup.</li>\n      <li>Transformations lazy, actions trigger. Skew = the #1 hidden killer.</li>\n      <li>Broadcast small dims, sort-merge big-vs-big.</li>\n      <li>Delta needs OPTIMIZE/VACUUM; time travel is a debugging superpower.</li>\n      <li>Unity Catalog = 3-level namespace + RBAC + lineage.</li>\n    </ul>\n  </div>",
  "words": 743
 },
 {
  "id": "m8",
  "title": "Orchestration",
  "subtitle": "DAGs, schedules, retries, SLAs — making \"run every morning\" reliable.",
  "html": "<p class=\"lead\">An orchestrator schedules and runs your pipeline, handling retries, alerts, and dependency order. Whether it's Airflow, Dagster, Prefect, or Databricks Workflows — the concepts are the same.</p>\n\n  <h3>The Universal Concepts</h3>\n  <table>\n    <tbody><tr><th>Concept</th><th>What it is</th></tr>\n    <tr><td><b>DAG</b></td><td>Directed Acyclic Graph — tasks + dependencies</td></tr>\n    <tr><td><b>Task / Operator</b></td><td>One unit of work (run SQL, call API, run dbt)</td></tr>\n    <tr><td><b>Schedule</b></td><td>When DAG runs (cron or interval)</td></tr>\n    <tr><td><b>Execution Date</b></td><td>The logical date the run represents (often \"yesterday\")</td></tr>\n    <tr><td><b>Retries</b></td><td>Auto-retry failed tasks N times with backoff</td></tr>\n    <tr><td><b>SLA</b></td><td>Max acceptable duration; breach = alert</td></tr>\n    <tr><td><b>Backfill</b></td><td>Re-run for historical dates</td></tr>\n    <tr><td><b>Idempotency</b></td><td>Same input → same output, every time</td></tr>\n  </tbody></table>\n\n  <h3>Airflow Mental Model</h3>\n  <pre><code><span class=\"cm\"># dags/daily_revenue.py</span>\n<span class=\"kw\">from</span> airflow <span class=\"kw\">import</span> DAG\n<span class=\"kw\">from</span> airflow.providers.databricks.operators.databricks <span class=\"kw\">import</span> DatabricksRunNowOperator\n<span class=\"kw\">from</span> datetime <span class=\"kw\">import</span> datetime, timedelta\n\ndefault_args = {\n    <span class=\"str\">'owner'</span>: <span class=\"str\">'data-team'</span>,\n    <span class=\"str\">'retries'</span>: <span class=\"num\">2</span>,\n    <span class=\"str\">'retry_delay'</span>: timedelta(minutes=<span class=\"num\">10</span>),\n    <span class=\"str\">'sla'</span>: timedelta(hours=<span class=\"num\">2</span>),\n}\n\n<span class=\"kw\">with</span> DAG(\n    dag_id=<span class=\"str\">'daily_revenue'</span>,\n    default_args=default_args,\n    schedule=<span class=\"str\">'0 6 * * *'</span>,  <span class=\"cm\"># every day 6am</span>\n    start_date=datetime(<span class=\"num\">2026</span>, <span class=\"num\">1</span>, <span class=\"num\">1</span>),\n    catchup=<span class=\"kw\">False</span>,\n    tags=[<span class=\"str\">'finance'</span>, <span class=\"str\">'daily'</span>],\n) <span class=\"kw\">as</span> dag:\n\n    ingest = DatabricksRunNowOperator(\n        task_id=<span class=\"str\">'ingest_salesforce'</span>,\n        job_id=<span class=\"num\">123</span>,\n    )\n    transform = DatabricksRunNowOperator(\n        task_id=<span class=\"str\">'dbt_build'</span>,\n        job_id=<span class=\"num\">456</span>,\n    )\n    publish = DatabricksRunNowOperator(\n        task_id=<span class=\"str\">'publish_dashboard'</span>,\n        job_id=<span class=\"num\">789</span>,\n    )\n\n    ingest &gt;&gt; transform &gt;&gt; publish  <span class=\"cm\"># dependency order</span></code></pre>\n\n  <h3>Idempotency — the non-negotiable property</h3>\n  <p>If your job runs twice for the same date, it should produce the same result. Otherwise retries and backfills corrupt your data.</p>\n  <ul>\n    <li><b>Bad</b>: <code>INSERT INTO fact_x VALUES ...</code> — duplicates on retry.</li>\n    <li><b>Good</b>: <code>MERGE INTO fact_x USING ... ON id = id</code> — upserts cleanly.</li>\n    <li><b>Better</b>: <code>DELETE FROM fact_x WHERE date = '{{ ds }}'; INSERT ...</code> — \"rebuild this date's slice.\"</li>\n    <li><b>Best</b>: dbt incremental with <code>unique_key</code> — dbt handles the MERGE.</li>\n  </ul>\n\n  <h3>Orchestrator Comparison</h3>\n  <table>\n    <tbody><tr><th></th><th>Airflow</th><th>Dagster</th><th>Prefect</th><th>DBX Workflows</th></tr>\n    <tr><td>Style</td><td>Task-first</td><td>Asset-first</td><td>Pythonic</td><td>Job-first</td></tr>\n    <tr><td>Strength</td><td>Mature, huge ecosystem</td><td>Asset lineage</td><td>Dev UX, dynamic</td><td>Native Databricks</td></tr>\n    <tr><td>Your stack</td><td>Yes (likely primary)</td><td>—</td><td>—</td><td>Yes (for dbt + Spark jobs)</td></tr>\n  </tbody></table>\n\n  <h3>Scheduling Patterns</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>Time-based</h4>\n      <p><code>0 6 * * *</code> — every day 6am UTC. Most common. Watch out for daylight savings.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Sensor-triggered</h4>\n      <p>Wait for upstream signal — file arrives in S3, partition appears, API publishes.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Event-driven</h4>\n      <p>Kafka message / S3 event triggers the run. Near-real-time.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Manual</h4>\n      <p>Backfills, ad-hoc reruns. Always have a button.</p>\n    </div>\n  </div>\n\n  <h3>Alerts &amp; SLAs</h3>\n  <ul>\n    <li><b>Failure alert</b> — task failed after all retries. Page on-call.</li>\n    <li><b>SLA miss</b> — task didn't finish in time. Soft alert.</li>\n    <li><b>Freshness check</b> — downstream-aware: \"did dim_accounts update today?\" dbt's <code>source freshness</code> tests this.</li>\n    <li>Route alerts to Slack channel + PagerDuty for true critical.</li>\n  </ul>\n\n  <h3>Flashcards</h3>\n  <div class=\"flashcard-set\">\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">What makes a pipeline idempotent?</div>\n      <div class=\"answer\">Running it N times with the same input parameters produces the same output. Requires MERGE/upsert semantics or \"delete-then-insert\" for the date partition — not bare INSERTs.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">When would you use a sensor over a cron schedule?</div>\n      <div class=\"answer\">When the upstream data arrival time is unpredictable — e.g., a Salesforce dump that might land 5–60 min after midnight. Sensor polls, fires when ready.</div>\n    </div>\n  </div>\n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>DAG = tasks + deps. Orchestrator handles schedule, retries, alerts, SLAs.</li>\n      <li>Idempotency is non-negotiable: MERGE or delete-then-insert.</li>\n      <li>Airflow is the industry default; Dagster gaining traction with asset-first model.</li>\n      <li>Sensors for unpredictable upstream; cron for fixed-time.</li>\n    </ul>\n  </div>",
  "words": 470
 },
 {
  "id": "m9",
  "title": "Ingestion & CDC",
  "subtitle": "How data gets into the warehouse — and why CDC is the silent killer of analyst metrics.",
  "html": "<p class=\"lead\">\"How does the data get here?\" Batch dumps, ELT tools, Kafka streams — each has trade-offs. CDC (Change Data Capture) is the AE-critical pattern: streaming row-level changes from prod databases into the lake.</p>\n\n  <h3>Ingestion Patterns</h3>\n  <table>\n    <tbody><tr><th>Pattern</th><th>How it works</th><th>Latency</th><th>Use case</th></tr>\n    <tr><td>Batch ETL</td><td>Daily SELECT * FROM prod → warehouse</td><td>24h</td><td>Small dims, historical data</td></tr>\n    <tr><td>Incremental batch</td><td>SELECT * WHERE updated_at &gt; last_run</td><td>1h-24h</td><td>Most cases</td></tr>\n    <tr><td><b>CDC streaming</b></td><td>Read DB binlog, stream changes</td><td>seconds</td><td>Near-real-time, high-volume</td></tr>\n    <tr><td>Event streams</td><td>Apps write events to Kafka, consumed by stream → lake</td><td>seconds</td><td>Behavioral data, telemetry</td></tr>\n    <tr><td>Managed ELT</td><td>Fivetran/Airbyte/Stitch handles it</td><td>15min-24h</td><td>SaaS sources (Salesforce, Stripe)</td></tr>\n  </tbody></table>\n\n  <h3>CDC Deep Dive</h3>\n  <p>CDC reads the database's transaction log (MySQL binlog, Postgres WAL, MongoDB oplog) and emits one event per row change: <code>insert</code>, <code>update</code>, <code>delete</code>.</p>\n\n  <h4>Typical CDC stack</h4>\n  <p>Prod DB (MySQL) → Debezium → Kafka → Spark Streaming → Delta Lake (Bronze) → dbt (Silver) → Marts (Gold).</p>\n\n  <h4>The CDC payload shape</h4>\n  <pre><code>{\n  <span class=\"str\">\"op\"</span>: <span class=\"str\">\"u\"</span>,                       <span class=\"cm\"># c=create, u=update, d=delete, r=read (snapshot)</span>\n  <span class=\"str\">\"ts_ms\"</span>: <span class=\"num\">1716000000000</span>,\n  <span class=\"str\">\"before\"</span>: { <span class=\"str\">\"id\"</span>: <span class=\"num\">1</span>, <span class=\"str\">\"status\"</span>: <span class=\"str\">\"active\"</span> },\n  <span class=\"str\">\"after\"</span>:  { <span class=\"str\">\"id\"</span>: <span class=\"num\">1</span>, <span class=\"str\">\"status\"</span>: <span class=\"str\">\"closed\"</span> },\n  <span class=\"str\">\"source\"</span>: { <span class=\"str\">\"db\"</span>: <span class=\"str\">\"prod\"</span>, <span class=\"str\">\"table\"</span>: <span class=\"str\">\"accounts\"</span> }\n}</code></pre>\n\n  <h3>The CDC Reality</h3>\n  <div class=\"callout danger\">\n    <div class=\"callout-title\">Critical knowledge — memorise</div>\n    <p>Raw kafka_cdc tables have <code>__is_deleted</code> column. <b>Missing this filter = production bug.</b> Required on:</p>\n    <p style=\"font-family:monospace;font-size:12px;color:var(--text);\"><code>job_assignments, accounts, sites, contacts, activation_keys, account_movements, subscriptions, ad_campaigns, site_abns, site_galleries, license_details</code></p>\n    <p>Always: <code>WHERE __is_deleted = false</code> (or <code>= 0</code> for raw kafka).</p>\n    <p>Also: rows with <code>active = 0</code> on these tables are <b>in-place updates</b>, not separate insert rows. Confirm mechanics before writing dedup logic.</p>\n  </div>\n\n  <h3>Soft Delete vs Hard Delete</h3>\n  <ul>\n    <li><b>Soft delete</b>: <code>UPDATE accounts SET is_deleted=true</code>. Row still in table.</li>\n    <li><b>Hard delete</b>: <code>DELETE FROM accounts</code>. Row gone — only CDC <code>op=d</code> events remember it existed.</li>\n    <li>CDC pipelines must apply tombstone logic for hard deletes — usually flag <code>__is_deleted = true</code> in the silver layer and let downstream filter.</li>\n  </ul>\n\n  <h3>Kafka 101</h3>\n  <table>\n    <tbody><tr><th>Concept</th><th>Meaning</th></tr>\n    <tr><td>Topic</td><td>Named log of messages (e.g. <code>prod.accounts.cdc</code>)</td></tr>\n    <tr><td>Partition</td><td>Topic split for parallelism. Ordering is per-partition.</td></tr>\n    <tr><td>Offset</td><td>Position of a message in a partition. Consumers track their offset.</td></tr>\n    <tr><td>Producer</td><td>Writes messages (Debezium, app code)</td></tr>\n    <tr><td>Consumer</td><td>Reads messages (Spark Streaming, downstream apps)</td></tr>\n    <tr><td>Consumer Group</td><td>Coordinated set of consumers; partition assigned to one consumer in group</td></tr>\n    <tr><td>Retention</td><td>How long messages stick around (e.g. 7 days)</td></tr>\n  </tbody></table>\n\n  <h3>Building a CDC Silver Model</h3>\n  <pre><code><span class=\"cm\">-- silver layer: latest non-deleted row per account_id</span>\n<span class=\"kw\">with</span> ranked <span class=\"kw\">as</span> (\n  <span class=\"kw\">select</span> *,\n    <span class=\"fn\">row_number</span>() <span class=\"kw\">over</span> (\n      <span class=\"kw\">partition by</span> account_id\n      <span class=\"kw\">order by</span> __op_ts <span class=\"kw\">desc</span>\n    ) <span class=\"kw\">as</span> rn\n  <span class=\"kw\">from</span> bronze.kafka_cdc_accounts\n)\n<span class=\"kw\">select</span> *\n<span class=\"kw\">from</span> ranked\n<span class=\"kw\">where</span> rn = <span class=\"num\">1</span>\n  <span class=\"kw\">and</span> __is_deleted = <span class=\"kw\">false</span>;</code></pre>\n\n  <h3>Schema Evolution</h3>\n  <p>Source DB adds a column → CDC payload changes shape → downstream models break. Strategies:</p>\n  <ul>\n    <li><b>Schema Registry</b> (Confluent) — central schema authority; producers/consumers validate.</li>\n    <li><b>Delta schema evolution</b>: <code>MERGE WITH SCHEMA EVOLUTION</code> auto-adds new columns.</li>\n    <li><b>Contracts</b> — explicit agreement upstream/downstream on field types &amp; required fields.</li>\n  </ul>\n\n  <h3>Flashcards</h3>\n  <div class=\"flashcard-set\">\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">What's the difference between batch incremental ingestion and CDC streaming?</div>\n      <div class=\"answer\">Batch incremental runs SELECTs at intervals (every hour, etc.) — misses deletes &amp; hard-to-detect updates if no <code>updated_at</code>. CDC reads the DB's binlog/WAL and emits every change in near-real-time, including deletes.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">Why is <code>__is_deleted</code> critical on kafka_cdc tables?</div>\n      <div class=\"answer\">Hard deletes in prod produce a CDC tombstone row with <code>__is_deleted = true</code>. The row stays in bronze for replayability. If you don't filter it out in silver/marts, you're counting deleted records — production correctness bug.</div>\n    </div>\n  </div>\n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>Ingestion spectrum: batch ETL → incremental batch → CDC streaming → events.</li>\n      <li>CDC = DB transaction log → Kafka → bronze; preserves inserts, updates, deletes.</li>\n      <li>Always filter <code>__is_deleted = false</code> on CDC tables. Always.</li>\n      <li>Schema evolution: registry + Delta SCHEMA EVOLUTION + contracts.</li>\n    </ul>\n  </div>",
  "words": 571
 },
 {
  "id": "m10",
  "title": "Data Quality & Testing",
  "subtitle": "Trust is built one assertion at a time.",
  "html": "<p class=\"lead\">A correct query running on bad data still gives a wrong answer. AE owns the assertions that protect the company from \"the dashboard is wrong\" Slack messages at 9pm.</p>\n\n  <h3>The 6 Dimensions of Data Quality</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>Completeness</h4>\n      <p>No missing rows or columns. \"Yesterday's orders should have ~1k rows; today says 12.\"</p>\n    </div>\n    <div class=\"card\">\n      <h4>Uniqueness</h4>\n      <p>Primary keys are actually unique. No duplicate user_ids.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Validity</h4>\n      <p>Values conform to format/type. Email looks like an email. Status ∈ allowed set.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Accuracy</h4>\n      <p>Values are right. Revenue matches Stripe / Salesforce / Zuora.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Consistency</h4>\n      <p>Same metric, same number across tools. SF vs Tableau vs dbt agree.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Timeliness</h4>\n      <p>Data is fresh — yesterday's data is available by 8am.</p>\n    </div>\n  </div>\n\n  <h3>Test Layers</h3>\n  <table>\n    <tbody><tr><th>Layer</th><th>What it checks</th><th>Tool</th></tr>\n    <tr><td>Source freshness</td><td>Did source update recently?</td><td>dbt source freshness</td></tr>\n    <tr><td>Schema tests</td><td>Unique, not_null, accepted_values, relationships</td><td>dbt generic tests</td></tr>\n    <tr><td>Data tests</td><td>Custom SQL assertions (e.g. revenue &gt; 0)</td><td>dbt singular tests</td></tr>\n    <tr><td>Anomaly detection</td><td>Row count today vs 7-day avg</td><td>dbt-expectations, Elementary, Monte Carlo</td></tr>\n    <tr><td>Reconciliation</td><td>Source row count = target row count</td><td>Custom dbt or scripts</td></tr>\n    <tr><td>Contracts</td><td>Schema/types of model match declaration</td><td>dbt model contracts</td></tr>\n  </tbody></table>\n\n  <h3>dbt Test Recipes</h3>\n  <pre><code><span class=\"cm\"># 1. Standard built-ins</span>\n<span class=\"kw\">columns:</span>\n  - <span class=\"kw\">name:</span> account_id\n    <span class=\"kw\">tests:</span>\n      - unique\n      - not_null\n\n<span class=\"cm\"># 2. dbt-utils row count delta</span>\n<span class=\"kw\">tests:</span>\n  - dbt_utils.equal_rowcount:\n      compare_model: ref(<span class=\"str\">'stg_accounts'</span>)\n\n<span class=\"cm\"># 3. dbt-expectations expression</span>\n  - dbt_expectations.expect_column_values_to_be_between:\n      column: revenue\n      min_value: 0\n      max_value: 1000000\n\n<span class=\"cm\"># 4. recency check</span>\n  - dbt_utils.recency:\n      datepart: day\n      field: created_at\n      interval: 1</code></pre>\n\n  <h3>Singular Tests (custom SQL)</h3>\n  <pre><code><span class=\"cm\">-- tests/no_negative_revenue.sql\n-- a singular test passes when it returns 0 rows</span>\n<span class=\"kw\">select</span> *\n<span class=\"kw\">from</span> <span class=\"fn\">{{ ref('fct_jobs') }}</span>\n<span class=\"kw\">where</span> revenue &lt; <span class=\"num\">0</span></code></pre>\n\n  <h3>Severity</h3>\n  <ul>\n    <li><code>error</code> (default) — blocks downstream models, alerts immediately.</li>\n    <li><code>warn</code> — logs but doesn't block. Useful for \"investigate later\" checks.</li>\n    <li><code>config.error_if</code> / <code>warn_if</code> — escalate based on row count thresholds.</li>\n  </ul>\n\n  <h3>The Test Pyramid for Data</h3>\n  <ol>\n    <li><b>Schema tests</b> (cheap, broad) — every model, every PK column</li>\n    <li><b>Business rule tests</b> (focused) — every fact's grain, every metric's bounds</li>\n    <li><b>Reconciliation tests</b> (cross-system) — daily SF vs warehouse row count</li>\n    <li><b>Anomaly tests</b> (statistical) — alert on unusual deltas</li>\n  </ol>\n\n  <h3>Data Contracts (the new hotness)</h3>\n  <p>Producer (source team) and consumer (AE/BI) agree on the schema of a \"shared\" table. Breaking changes require migration, not \"oh sorry the column is gone.\"</p>\n  <pre><code><span class=\"kw\">models:</span>\n  - <span class=\"kw\">name:</span> dim_accounts\n    <span class=\"kw\">config:</span>\n      <span class=\"kw\">contract:</span>\n        <span class=\"kw\">enforced:</span> true\n    <span class=\"kw\">columns:</span>\n      - <span class=\"kw\">name:</span> account_id\n        <span class=\"kw\">data_type:</span> bigint\n        <span class=\"kw\">constraints:</span>\n          - <span class=\"kw\">type:</span> not_null\n          - <span class=\"kw\">type:</span> primary_key</code></pre>\n\n  <h3>Observability Tools</h3>\n  <table>\n    <tbody><tr><th>Tool</th><th>Niche</th></tr>\n    <tr><td>Elementary</td><td>Open-source, dbt-native anomaly detection + alerting</td></tr>\n    <tr><td>Monte Carlo</td><td>Enterprise data observability — auto-discovery</td></tr>\n    <tr><td>Bigeye / Soda / Anomalo</td><td>Similar enterprise space</td></tr>\n    <tr><td>Great Expectations</td><td>Python-first data validation framework</td></tr>\n  </tbody></table>\n\n  <h3>Flashcards</h3>\n  <div class=\"flashcard-set\">\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">When should a test be severity <code>warn</code> rather than <code>error</code>?</div>\n      <div class=\"answer\">When the failure is informative but doesn't make downstream data wrong — e.g. \"row count down 20%\" might be valid (long weekend). For correctness violations (unique, not_null on PK), use error.</div>\n    </div>\n    <div class=\"flashcard\" onclick=\"this.classList.toggle('flipped')\">\n      <div class=\"hint\">Click</div>\n      <div class=\"question\">What's a singular test in dbt?</div>\n      <div class=\"answer\">A custom SQL file in <code>tests/</code> that returns rows on failure. Anything that returns &gt;0 rows = test fail. Useful for business rules that don't fit generic tests.</div>\n    </div>\n  </div>\n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>6 DQ dims: completeness, uniqueness, validity, accuracy, consistency, timeliness.</li>\n      <li>dbt tests: schema (built-in), singular (custom SQL), dbt-utils, dbt-expectations.</li>\n      <li>Severity: error (block + alert) vs warn (log only).</li>\n      <li>Contracts: enforce schema between producer and consumer.</li>\n    </ul>\n  </div>",
  "words": 506
 },
 {
  "id": "m11",
  "title": "System Design for Data",
  "subtitle": "Architecting for scale, change, and the inevitable retro.",
  "html": "<p class=\"lead\">Senior AEs/DEs design systems. Given a vague brief (\"we need real-time customer insights\"), you decompose into ingestion, storage, transformation, serving, governance — and defend the trade-offs.</p>\n\n  <h3>The Lakehouse Reference Architecture</h3>\n  <pre><code>┌──────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐\n│   SOURCES    │ → │  INGESTION   │ → │  LAKEHOUSE   │ → │ TRANSFORMS   │ → │   SERVING    │\n│              │   │              │   │              │   │              │   │              │\n│ OLTP DBs     │   │ CDC          │   │  BRONZE      │   │ dbt          │   │ BI / Tableau │\n│ APIs (SF)    │   │ Fivetran     │   │  ↓ raw delta │   │ Spark SQL    │   │ Reverse ETL  │\n│ Events       │   │ Kafka stream │   │  SILVER      │   │ PySpark      │   │ APIs         │\n│ Files (S3)   │   │ Batch jobs   │   │  ↓ cleaned   │   │              │   │ ML features  │\n│              │   │              │   │  GOLD        │   │              │   │              │\n└──────────────┘   └──────────────┘   │  ↓ marts     │   └──────────────┘   └──────────────┘\n                                       └──────┬───────┘\n                                              ↓\n                                       ┌──────────────┐\n                                       │  GOVERNANCE  │\n                                       │              │\n                                       │ Catalog      │\n                                       │ Lineage      │\n                                       │ DQ tests     │\n                                       │ Contracts    │\n                                       │ Access ctrl  │\n                                       └──────────────┘</code></pre>\n\n  <h3>Trade-Off Frameworks to Reach For</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>Latency vs Cost</h4>\n      <p>Real-time = streaming + always-on compute = $$$. Hourly batch = cheap. Map each use case to its real latency SLO.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Schema-on-Read vs Write</h4>\n      <p>Bronze tolerates messy raw; gold enforces schema. Push validation as far left as cheap.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Build vs Buy</h4>\n      <p>Fivetran (~$$$) vs custom Python ingestion (eng time). For SaaS sources: buy. For internal volume: build.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Push vs Pull</h4>\n      <p>Push = producers publish to a stream (eventual consistency, decoupled). Pull = consumer queries source (simple, can hammer prod).</p>\n    </div>\n    <div class=\"card\">\n      <h4>Normalized vs Denormalized Serving</h4>\n      <p>BI loves wide tables; ML wants long. Often both — gold has narrow facts + wide reporting models.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Centralized vs Decentralized</h4>\n      <p>Central platform team vs Data Mesh (each domain owns its data product). Hybrid is reality.</p>\n    </div>\n  </div>\n\n  <h3>Capacity Planning — the back-of-envelope sniff test</h3>\n  <ul>\n    <li>1B rows × 100 bytes = 100 GB raw. Parquet compresses ~5–10× → 10–20 GB on disk.</li>\n    <li>Scan throughput on Photon ≈ 1–3 GB/sec/worker. 20 GB table on 10-worker cluster → ~1s scan with filters.</li>\n    <li>Kafka throughput per partition ≈ 10 MB/s. Need 100 MB/s? ~10+ partitions.</li>\n    <li>dbt run time scales with model count + warehouse size. Slim CI cuts this 10×.</li>\n  </ul>\n\n  <h3>System Design Interview Template</h3>\n  <ol>\n    <li><b>Clarify</b> — what's the latency SLO, volume, accuracy bar, who consumes?</li>\n    <li><b>Pick architecture</b> — batch / streaming / lakehouse / lambda? Justify.</li>\n    <li><b>Sketch flow</b> — sources → ingestion → storage → transform → serve.</li>\n    <li><b>Schema</b> — fact/dim, grain, key choice, SCDs.</li>\n    <li><b>Operational</b> — orchestration, monitoring, alerts, DQ tests, contracts.</li>\n    <li><b>Trade-offs</b> — what did you simplify? What would v2 look like at 10× scale?</li>\n  </ol>\n\n  <h3>Worked Example: \"Real-time contractor response-time dashboard\"</h3>\n  <div class=\"callout\">\n    <div class=\"callout-title\">Brief</div>\n    Show contractors how their median lead-response-time compares to peers, refreshed within 5 minutes of every action.\n  </div>\n  <ul>\n    <li><b>Sources</b>: lead-issued event, contractor-response event. Both already on Kafka.</li>\n    <li><b>Ingestion</b>: Spark Structured Streaming reads both topics → write to bronze.<br>5-min trigger, exactly-once via Delta + idempotent Kafka offsets.</li>\n    <li><b>Storage</b>: bronze raw events; silver enriched with contractor + category dims (joined at stream-time); gold = rolling 30-day response time agg per contractor + category.</li>\n    <li><b>Transform</b>: dbt incremental on silver→gold every 5 min via Workflows.</li>\n    <li><b>Serve</b>: DBSQL Serverless warehouse, AI/BI dashboard with auto-refresh; or push to Postgres via reverse ETL for in-product widget.</li>\n    <li><b>DQ</b>: unique on (contractor_id, lead_id, event_ts); not_null on contractor_id; row count delta vs 1h ago must be within 30%.</li>\n    <li><b>Cost trade-off</b>: streaming all the way is expensive — 5-min micro-batch with serverless gives 90% of value at 30% of cost.</li>\n  </ul>\n\n  <h3>Anti-Patterns to Avoid in Design</h3>\n  <ul>\n    <li><b>Premature streaming</b> — daily batch is enough; streaming adds 10× complexity.</li>\n    <li><b>One big table</b> — denormalised \"kitchen sink\" looks easy; impossible to evolve.</li>\n    <li><b>No clear ownership</b> — model has no owner = stale + wrong within 6 months.</li>\n    <li><b>Missing contracts</b> — source team changes column, your model breaks at 3am.</li>\n    <li><b>Untested upserts</b> — MERGE logic that introduces duplicates on edge cases.</li>\n  </ul>\n\n  \n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>Lakehouse = source → ingestion → bronze/silver/gold → serving + governance.</li>\n      <li>Always state SLOs (latency, freshness, accuracy) before picking architecture.</li>\n      <li>Streaming costs 10× more than batch — match latency to actual need.</li>\n      <li>Capacity planning: GB/s scan, MB/s Kafka partitions, dbt model count × size.</li>\n      <li>Anti-patterns: premature streaming, kitchen-sink tables, no ownership, no contracts.</li>\n    </ul>\n  </div>",
  "words": 708
 },
 {
  "id": "m12",
  "title": "Interview Prep & Portfolio",
  "subtitle": "How to demonstrate AE competence without already having the title.",
  "html": "<p class=\"lead\">Hiring managers want to see code, not certificates. The fastest path: a public dbt project on GitHub + a writeup. The interview is then mostly \"walk me through it.\"</p>\n\n  <h3>The 4 Interview Loops You'll Face</h3>\n  <table>\n    <tbody><tr><th>Loop</th><th>What they test</th><th>How to prep</th></tr>\n    <tr><td>SQL screen</td><td>Window functions, dedup, fan-out, NULLs</td><td>StrataScratch, DataLemur, Mode SQL Tutorial</td></tr>\n    <tr><td>dbt/modeling</td><td>Star schema design, SCDs, incremental, tests</td><td>Build a public dbt project end-to-end</td></tr>\n    <tr><td>System design</td><td>\"Design a [analytics platform / pipeline / metric]\"</td><td>Read Designing Data-Intensive Apps, Practice with the template above</td></tr>\n    <tr><td>Behavioral</td><td>Stakeholder mgmt, ambiguity, prioritisation</td><td>STAR stories from your analyst experience — you have a lot to draw on</td></tr>\n  </tbody></table>\n\n  <h3>Portfolio Project: production-style public dbt repo</h3>\n  <p>Pick a public dataset (NYC taxi, Olist e-commerce, IMDb, Spotify-charts). Build:</p>\n  <ol>\n    <li>Source declaration + freshness</li>\n    <li>3 layers: staging / intermediate / marts</li>\n    <li>At least one snapshot (SCD2) on a slowly-changing entity</li>\n    <li>At least one incremental model with proper unique_key + watermark</li>\n    <li>20+ tests across the project</li>\n    <li>dbt docs site published via GitHub Pages</li>\n    <li>README with: ERD, lineage screenshot, decision log (why these grains), how to run</li>\n    <li>GitHub Actions CI: lint + dbt compile + dbt build state:modified+</li>\n  </ol>\n\n  <h3>Behavioral: STAR stories you can already tell</h3>\n  <ul>\n    <li><b>Ambiguous request</b> — \"Sales asked for X, I clarified into Y, delivered Z\"</li>\n    <li><b>Trust breach</b> — \"Numbers disagreed; I traced root cause to grain mismatch\"</li>\n    <li><b>Cross-team blocker</b> — \"I worked with eng to add a column to source\"</li>\n    <li><b>Performance fix</b> — \"Tableau viz timed out; pre-aggregated in dbt, 50× faster\"</li>\n    <li><b>Mistake</b> — \"I shipped wrong metric, learned to add tests\"</li>\n  </ul>\n\n  <h3>SQL Practice — the 20 patterns to drill</h3>\n  <ol>\n    <li>Find the second-highest salary per department.</li>\n    <li>Running total per customer.</li>\n    <li>Cumulative count of distinct users by day.</li>\n    <li>Sessionization: group events into sessions when gap &gt; 30 min.</li>\n    <li>Top N per group.</li>\n    <li>Dedup keeping latest by timestamp.</li>\n    <li>Find customers who placed orders in two consecutive months.</li>\n    <li>Active users yesterday but not today.</li>\n    <li>Rolling 7-day moving average.</li>\n    <li>Percent of total per group.</li>\n    <li>Median per group (without MEDIAN function).</li>\n    <li>Funnel: conversion at each step.</li>\n    <li>Retention cohort: signup_month vs active_month.</li>\n    <li>Gaps and islands: consecutive date ranges.</li>\n    <li>Self-join: manager/employee hierarchy.</li>\n    <li>Pivot fact data by category.</li>\n    <li>Anti-join: customers with no order.</li>\n    <li>Detect duplicates and quantify them.</li>\n    <li>Reconciliation: SUM(a) per id vs SUM(b) per id, find mismatches.</li>\n    <li>Window with frame: 30-day rolling SUM via <code>ROWS BETWEEN INTERVAL …</code>.</li>\n  </ol>\n\n  <h3>Books &amp; Resources Worth the Time</h3>\n  <div class=\"card-grid\">\n    <div class=\"card\">\n      <h4>\"The Data Warehouse Toolkit\" — Kimball</h4>\n      <p>The Bible for dimensional modeling. Read chapters 1-3 + your industry chapter.</p>\n    </div>\n    <div class=\"card\">\n      <h4>\"Fundamentals of Data Engineering\" — Joe Reis / Matt Housley</h4>\n      <p>The modern overview. Read cover to cover.</p>\n    </div>\n    <div class=\"card\">\n      <h4>\"Designing Data-Intensive Applications\" — Kleppmann</h4>\n      <p>Deep on distributed systems. Skim chapters 1-4 + 10 for AE.</p>\n    </div>\n    <div class=\"card\">\n      <h4>dbt Learn (free courses)</h4>\n      <p>\"Fundamentals\", \"Advanced Materializations\", \"Jinja, Macros\". Official, gold standard.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Locally Optimistic blog</h4>\n      <p>AE practitioners writing about real problems. Searchable archive.</p>\n    </div>\n    <div class=\"card\">\n      <h4>Databricks Academy</h4>\n      <p>Free, self-paced. Get the \"Databricks Certified Data Engineer Associate\" certification.</p>\n    </div>\n  </div>\n\n  <h3>Certifications — worth it?</h3>\n  <ul>\n    <li><b>Databricks Certified Data Engineer Associate</b> — yes, signals capability + free study material.</li>\n    <li><b>dbt Analytics Engineering Certification</b> — yes, aligned exactly with this transition.</li>\n    <li>Cloud certs (AWS Data Analytics, GCP Data Engineer) — nice-to-have, not critical.</li>\n    <li>Skip \"data analyst\" certs — you're moving on.</li>\n  </ul>\n\n  <h3>Your 30-Day Action Plan (from today)</h3>\n  <table>\n    <tbody><tr><th>Week</th><th>Action</th></tr>\n    <tr><td>1</td><td>Build SQL muscle: 30 problems on DataLemur/StrataScratch (window fns, dedup, fan-out)</td></tr>\n    <tr><td>2</td><td>Scaffold the public dbt project — pick dataset, deploy to free Snowflake/Databricks Community</td></tr>\n    <tr><td>3</td><td>Build 3-layer + snapshots + incremental + 20 tests; publish dbt docs</td></tr>\n    <tr><td>4</td><td>Write the README + post on LinkedIn; do 3 mock system design interviews</td></tr>\n  </tbody></table>\n\n  <h3>What to Negotiate Going Internal vs External</h3>\n  <ul>\n    <li><b>Internal</b> — fastest path. You know the domain. Pitch: \"I'll shadow the dbt project's repo PRs for 4 weeks, then own one model end-to-end.\"</li>\n    <li><b>External</b> — bigger pay bump typically. Public portfolio + LinkedIn updates trigger inbound recruiters.</li>\n    <li>Title to target: <em>Analytics Engineer</em> or <em>Senior Data Analyst, Analytics Engineering track</em>. Some companies bundle as \"Data Engineer.\"</li>\n  </ul>\n\n  <div class=\"recap\">\n    <div class=\"recap-title\">Recap</div>\n    <ul>\n      <li>4 interview loops: SQL screen, dbt/modeling, system design, behavioral. You have analyst stories already.</li>\n      <li>Public dbt project + README + GitHub Actions CI = your portfolio.</li>\n      <li>Drill 20 SQL patterns; read Kimball ch.1-3 + Joe Reis cover-to-cover.</li>\n      <li>30-day plan: SQL → project → tests/docs → publish &amp; mock interviews.</li>\n      <li>Internal move is the lowest-friction first step.</li>\n    </ul>\n  </div>",
  "words": 690
 }
]